{
  "name": "@cayla-retail/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "//gen-types": [
    "NO CORRAS ESTO TODAVIA (05-sep-2026). Produccion aun no tiene comprobantes.items",
    "ni actualizar_transmision_comprobante: Felipe pega el archivo 23 de facturacion hoy.",
    "Regenerar ANTES de ese pegado borra las declaraciones de src/types.ts que el codigo",
    "de Facturacion ya usa (p_items, actualizar_transmision_comprobante) y rompe el build.",
    "Regenera DESPUES de pegar el 23, y verifica que las dos sigan estando.",
    "Dos correcciones al comando: el project-id era ntuucqlkyxqbedoakrvc, que ya no existe",
    "en la cuenta; y faltaba --schema retail, porque retail vive dentro del proyecto de",
    "cayla-dynamic (sin el flag genera los tipos de public, que es el esquema de Dynamic).",
    "El '>' de la shell trunca src/types.ts antes de que el comando falle: si el comando",
    "esta mal, no te avisa, te deja el archivo vacio."
  ],
  "scripts": {
    "gen-types": "supabase gen types typescript --project-id vovjyyiafkxteijimpuy --schema retail > src/types.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
