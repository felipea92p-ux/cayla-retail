{
  "name": "@cayla-retail/database",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "gen-types": "supabase gen types typescript --project-id vovjyyiafkxteijimpuy --schema retail > src/types.produccion.ts && echo Generado en src/types.produccion.ts (NO se versiona). src/types.ts tiene parches a mano: compara con \"git diff --no-index src/types.ts src/types.produccion.ts\" y traslada solo lo que cambio, respetando la lista de la cabecera.",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.6.0"
  }
}
