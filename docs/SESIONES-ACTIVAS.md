# Sesiones activas — CAYLA Retail

Tablero simple para que nadie lance una sesión de IA (o empiece una tarea grande) sin ver qué está tocando otra persona ahora mismo. Antes de lanzar una sesión nueva o empezar algo de más de 30 minutos: revisa esta lista. Al terminar o pausar: muévete a "Cerradas hoy".

Nace del incidente del 2026-09-17: 6 colisiones de numeración de ADR, 2 migraciones con el mismo timestamp, y una función (rechazar propuesta de color) construida dos veces en paralelo — todo por no tener este tablero. Ver el documento completo en [El Método CAYLA](../docs/BITACORA.md) y la entrada de hoy en BITACORA.md.

## Cómo usarlo

1. Antes de lanzar una sesión de IA o empezar una tarea de más de 30 minutos: agrégate abajo con una fila.
2. Si ves que alguien ya está tocando lo mismo que ibas a tocar (misma tabla, mismo módulo, mismo rango de ADR/migración): para y coordina antes de seguir, no asumas que "no va a chocar".
3. Al terminar: mueve tu fila a "Cerradas hoy" (no la borres — así queda el historial del día a la vista de todos).
4. Al día siguiente, "Cerradas hoy" se limpia. El historial real y permanente vive en `BITACORA.md`, este archivo es solo el estado de "ahora mismo".

## Activas ahora

| Quién | Rama / worktree | Qué está tocando | Desde |
|---|---|---|---|
| Claude (worktree `fotos-fondo-blanco`) | `claude/fotos-fondo-blanco` | **Fotos de prenda sin fondo y del mismo tamaño (ADR-0228).** Sin migración. Toca `lib/producto-fotos.ts` (`subirFotoProducto` gana `original`), `lib/fotos-pendientes.ts`, `components/FotosProducto.tsx`, `components/alta-producto/FotosAlta.tsx` (`FotoPendiente` gana `original`), **dos líneas de `NuevoProductoForm.tsx`** (pasa el original al subir y al encolar), el `matcher` de `proxy.ts`; nuevos `lib/foto-encuadre.ts`, `lib/preparar-foto.ts`, `components/RevisarFotosModal.tsx`, `public/quitar-fondo.worker.js`. **Quien toque la subida de fotos o `NuevoProductoForm`: avisar.** | 2026-09-26 |
| Claude (worktree `optimize-cayla-retail-team-03c071`) | `claude/quantities-inventory-sync-36b320` | **Nuevo producto con su stock de hoy (ADR-0212).** Paso 5 «Cuántas tienes hoy» en `/productos/nuevo`: migración `20260926130000` (`crear_producto_con_stock_inicial` + `fn_cargar_stock_inicial`, interna; **llama** a `crear_producto_con_variantes` y `bajar_al_piso`, no las copia), `NuevoProductoForm.tsx`, `alta-producto/MatrizCantidades.tsx` (nuevo), `FichaPrevia.tsx`, `ProductoCreado.tsx`, `lib/alta-producto.ts`, prueba `scripts/pruebas/alta_con_stock_inicial.mjs` (+ CI). **SQL EN PRODUCCIÓN desde el 2026-09-26 (`20260926000932`); falta fusionar el PR para publicar la web y recargar las tablets.** Quien cambie la firma de `crear_producto_con_variantes`: avisar (la prueba F1 lo detecta). | 2026-09-26 |
| Claude (worktree `finanzas-module-breakdown-135bbb`) | `claude/finanzas-f2b-activos` (F2b; F1 y F2a ya en producción) | **Finanzas, construcción por fases (ADR-0195).** F2b: migración `20260925000000` (`activos_fijos` + `tipos_activo`, `gastos_fijos` + `gastos.gasto_fijo_id`, `fn_comprobante_y_pago`, `registrar_gasto` con firma nueva, `registrar_activo`, candados de egresos con activos). Web: `GastosPanel.tsx`, `GastosFijosYActivos.tsx` (nuevo), `RegistrarGastoModal.tsx`, `lib/gastos*.ts`, `PorPagarLista.tsx`, `CompraDetalle*.tsx`. **Quien toque `registrar_gasto`, `compras` o `activos_fijos`: avisar.** Sigue F3 (cuentas de dinero). | 2026-09-24 |
| Claude (worktree `busy-margulis-f4460e`) + 3 agentes | `claude/responsable-y-roles-spike` (principal), `claude/adr-0162-f3-actor`, `claude/adr-0162-f4a-terminales`, `claude/adr-0161-f4b-responsable` | **Terminales sin persona + combo Responsable (ADR-0161/0162, aprobados por Felipe).** Toca: migraciones `20260923010000`/`20260923100000` (`retail.terminales`, `fn_actor_persona_id`, reemplazo en ~65 funciones que buscan `personas.auth_user_id = auth.uid()`), `lib/persona-actual.ts`, pie del menú, `/colaboradores` pestaña Terminales, y el combo Responsable en Vender/Caja/Cambios/Devoluciones/Facturación/Inventario/Catálogo. **Si vas a recrear una función de retail que guarda algo, avisa: F3 la inyecta.** Pruebas en la base `cayla_f2` del contenedor (no en `postgres`). | 2026-09-22 |
| Claude (worktree `pantalla-ebc078`) | `claude/pantalla-ebc078` | **Productos (`/productos`): análisis `/pantalla` y tareas #1–#4** (ADR-0151): migración `20260922120000` (mismas firmas de `fn_productos` y `fn_productos_resumen`; **sin aplicar en producción**), prueba `scripts/pruebas/productos_alertas_de_stock.mjs` (+ paso en `ci.yml`), `ProductosGrilla.tsx`, `ProductosAgrupados.tsx`, `page.tsx` de `/productos`, `lib/productos-stock.ts` y `components/NotaStockTotal.tsx` (nuevos), `components/ui/Chip.tsx` (prop `tachado`), `docs/pantallas/productos.md`. **Quien toque esas funciones o esos componentes: avisar antes.** Listo para revisión; sin PR. | 2026-09-22 |
| Claude (worktree `youthful-nightingale-464864`) | `claude/colaboradores-rediseno` | Rediseño de `/colaboradores` sobre la maqueta de Felipe (ADR-0148): pestañas Activos/Suspendidos/Inactivas/Actividad, Suspender, Cambiar ubicación, alta múltiple. Toca `ColaboradoresPanel.tsx` (reescrito), `ColaboradoresTablas.tsx`/`ColaboradoresModales.tsx`/`ui/MenuAcciones.tsx` (nuevos), `lib/colaboradores*.ts`, `vender/historial/page.tsx` (una línea: el filtro vendedor), `packages/database/src/types.ts`, migración `20260922110000_colaboradores_suspender_y_actividad.sql`. **No toca `AppShell.tsx` ni `lib/menu.ts`.** | 2026-09-22 |
| Claude (worktree `github-remote-check-3ae391`) | `claude/colaboradores-auditoria` | Arreglos de la auditoría de `/colaboradores` (ADR-0145, `docs/pantallas/colaboradores.md`): `ColaboradoresPanel.tsx`, `lib/error-escritura.ts` (una huella), migración `20260922100000_colaboradores_endurecimiento.sql` (revoca escritura directa a `retail.colaboradores`, CHECK de ubicación, `agregar_/quitar_colaborador` avisan), prueba `scripts/pruebas/colaboradores_endurecimiento.mjs`. **No toca `AppShell.tsx` ni `lib/menu.ts`.** | 2026-09-22 |
| Claude (worktree `brave-northcutt-7a1d2c`) | `claude/cayla-menu-pajaros-aviario-1f2c50` (menú) y `claude/candado-lider-caja-ajuste` (candado) | **Menú del mostrador + aviario completo, rumbo a salir en vivo en TRU** (decisiones de Felipe del 2026-09-21). **Candado de líder D-13 (ADR-0143) — [PR #218](https://github.com/felipea92p-ux/cayla-retail/pull/218) fusionado y migración APLICADA en producción el 2026-09-21**: migración `20260921120000` (`cerrar_caja` y `registrar_movimiento` exigen `fn_es_lider()`), prueba `scripts/pruebas/candado_lider_caja_y_ajuste.mjs` (+ paso en `ci.yml`), escenario C2 de `scripts/caja/verificar.sql`, y cinco botones escondidos a quien no es líder (`CajaAbiertaPanel.tsx`, `PuntoDeVenta.tsx`, `InventarioPanel.tsx`, `ProductosAgrupados.tsx`, `ProductosGrilla.tsx`; una línea cada uno). **Menú a datos (ADR-0144): [PR #232](https://github.com/felipea92p-ux/cayla-retail/pull/232), sin cambio visible.** Sigue, con el ok de Felipe: «Más» + avatar «Yo» + lupa en celular, colaborador plano, «+ Nuevo» agrupado e Inicio por perfil, y los nombres («… del Taller»). Toca `AppShell.tsx`, `lib/menu.ts` (nuevo), `lib/produccion-menu.ts`, `menu.test.ts` y `menu-hoy.golden.json`. **Desde que entre #232, quien agregue una fila al menú edita `lib/menu.ts` (NO `AppShell.tsx`) y regenera `menu-hoy.golden.json` a propósito, explicando en su PR qué perfil ve algo distinto** (cómo: ADR-0144). #231 (Resumen de Producción) ya entró y quedó portado a `lib/menu.ts`; los PRs abiertos que editan el menú —#170 y #151— tendrán que portar sus filas; #66 quedó obsoleto. Producción SUPERA el tope de 6 hijas (7 desde #231): deuda registrada en `menu.test.ts`. | 2026-09-21 |
| Claude (worktree `billing-design-analysis-ee464b`) | `claude/billing-design-analysis-ee464b` | Facturación en cuatro vistas: **R1 (estructura, 2026-09-19) y las cuatro vistas con su look (R2 A a C, R3, R4; hasta el 2026-09-21) hechas y verificadas y la rama subida a `origin/claude/billing-design-analysis-ee464b` (2026-09-21, fusionada con `main` en `bdf7ea71`)**; falta el PR y su fusión a `main`, que necesita el OK de Felipe. La migración `20260921103000` de `fn_ventas_del_dia` (una fila por venta y sin anuladas; la leen también Caja y Vender) está pegada en producción (2026-09-21). La `20260921121500` (`anular_venta` libera el comprobante pendiente de la venta que anula) también está pegada en producción (2026-09-21). La `20260921161500` (un comprobante no nace sobre una venta anulada: trigger en `comprobantes`) también está pegada en producción (2026-09-21). La franja de proformas del Resumen (`FranjaProformas.tsx`, R2) también está hecha. Falta el cierre (ver BACKLOG). Spec aprobado por Felipe (2026-09-19); plan `docs/superpowers/plans/2026-09-19-facturacion-cuatro-vistas-r0-r1.md`. Toca `app/(app)/vender/facturacion/**` (layout + 4 vistas), `app/(app)/vender/descuentos/page.tsx` (se mueve), `next.config.ts` (un redirect), `ComprobantesPanel.tsx` y `ProformasPanel.tsx` (extraer modales), `lib/comprobantes.ts`, `lib/proformas.ts`, `lib/proformas-reglas.ts`, `lib/persona-actual.ts` (un `exigirLider`), `lib/facturacion-reglas.ts`, `lib/useFacturacionAcciones.ts`, `lib/ventas-comparativo.ts` y `lib/useTransmitir.ts` (nuevos), `components/ui/TarjetaKpiVidrio.tsx`, `HiloComprobante.tsx` y `BotonCompacto.tsx` (nuevos), `components/ui/Chip.tsx` (el tono «apagado» sube de contraste: lo usa también Compras), `components/FacturacionShell.tsx`, `FacturacionCabecera.tsx`, `FacturacionPestanas.tsx`, `SelectorMesFacturacion.tsx`, `EmitirComprobanteModal.tsx`, `NuevaProformaModal.tsx`, `ResumenTarjetas.tsx`, `ActividadDeHoy.tsx`, `FranjaProformas.tsx`, `ComprobantesTarjetas.tsx`, `ProformasTarjetas.tsx`, `CodigosTarjetas.tsx`, `MarcaDeCarga.tsx` y `SinCoincidencias.tsx` (nuevos), y las reglas `lib/facturacion-*-reglas.ts`, `facturacion-busqueda.ts`, `useFacturacionBusqueda.ts`, `ultima-carga-facturacion.ts` (nuevas; `fechas-lima.ts` gana `diaYHoraLima`), `supabase/migrations/20260921103000_ventas_del_dia_una_fila_por_venta_y_sin_anuladas.sql`, `supabase/migrations/20260921121500_anular_venta_libera_el_comprobante_pendiente.sql`, `supabase/migrations/20260921161500_comprobante_no_nace_sobre_venta_anulada.sql`, `scripts/pruebas/ventas_del_dia.mjs`, `anular_venta_comprobante.mjs` y `comprobante_venta_anulada.mjs` (con sus scripts en `package.json` y sus pasos en `.github/workflows/ci.yml`), `app/api/lucode/emitir/route.ts` y `lib/transmision-reglas.ts` (no transmite el comprobante de una venta anulada), `VenderNav.tsx` (se borra) y un bloque nuevo en `globals.css` (dentro de `@layer components`, ADR-0105). Se apoya en `CifraAnimada`, `FechaHoraLima`, `anim-sube` y `check-trazo` de Atelier, que ya está en `main` (ADR-0123, antes 0106, que chocaba con el de «colores»). ADR: **0124** (nació como 0113; pasó al 0121 y luego al 0124 porque la rama de Atelier —`interface-recommendations-8ce365`, ya en `main`— ocupó del 0121 al 0123). | 2026-09-18 |
| Claude (worktree `great-wing-4af135`) | `claude/panel-comercial` y `claude/hola-baee84` | **Panel comercial** `/comercial` (ADR-0110; `fn_comercial_*`, `comercial-reglas.ts`, `PanelComercialVista.tsx`, `packages/database` types a mano; **sin entrada en el menú**: falta decidir el módulo de roles, ver BACKLOG) y **candado solo-RPC de ventas/devoluciones** (ADR-0108; migración `20260918190000`) + **modelo contable** (ADR-0109, aprobado). **Números reservados: ADR-0109, 0110, 0119 (candado; era 0112); migraciones `20260918190000` y `20260918191000`.** (El ADR-0108 que reservé antes lo tomó otra sesión; renumerado.) Rama `claude/panel-calidad` (PR #167; Calidad: `fn_calidad*`, `calidad-reglas.ts`, `PanelCalidadVista.tsx`, `/comercial/calidad`; migraciones `20260918191500` y `20260918192000`; **ADR-0214**, antes 0113, que ya usa «Recibir por envío»). | 2026-09-18 |
| Claude (worktree `ventas-visual-redesign-240e2b`) | `claude/ventas-visual-redesign-240e2b` | Rediseño visual de Ventas: Caja/Punto de Venta/Cambios sobre el sistema visual ya vigente (ADR-0012). `/caja` y `/cambios` cerrados y verificados; Punto de Venta sigue. Toca `CajaAbiertaPanel.tsx`, `CajaGraficos.tsx`, `caja-panel-reglas.ts`, `lib/caja.ts`, `app/actions/caja.ts`, `lib/ubicaciones.ts`, `CambiosLista.tsx`, `CambioFormV2.tsx`, `BuscarPorComprobante.tsx`, `cambios-reglas.ts`, `cambios-estadisticas.ts` (nuevo), `lib/ventas-v2.ts`, `lib/panel-serie.ts`, migración `20260918091000` (`ubicaciones.meta_venta_diaria`, renombrada por choque con `resumen_inventario` de `main`), `globals.css` (tokens `--color-metodo-*`). Fusionada con `main` (PR #120-#122) sin choques de código, solo en BACKLOG/BITACORA. PR [#128](https://github.com/felipea92p-ux/cayla-retail/pull/128) abierto (Caja+Cambios), Punto de Venta sigue en esta misma rama. | 2026-09-18 |
| Felipe (Claude) | `claude/interface-recommendations-8ce365` | Rediseño de `/cambios` (ADR-0125) y de `/devoluciones` con el mismo modelo (ADR-0122): flujo guiado en 4 pasos, buscador único, actividad de 15 días; Devoluciones con «Por aprobar» y varias prendas por devolución. **Cambios:** migración `20260919000100` (antes `20260918150000`; `cambios.motivo`/`condicion`, `prendas_danadas.cambio_id`, firma nueva de `registrar_cambio` con drop+create) **aplicada en la base local compartida y en producción (2026-09-19, verificada) — el front ya puede fusionarse**. **Devoluciones: solo pantalla, sin migración.** Piezas compartidas: `lib/ventas-v2.ts` (`getVentasRecientes`), `cambios-reglas.ts`, `devoluciones-reglas.ts`, `FlujoGuiado`/`ComprasAgrupadas`/`BuscadorVentas` (componentes nuevos; reemplazan `CambiosLista`/`CambioFormV2`/`DevolucionesLista`/`DevolucionFormV2`), `ui/Chip`/`ui/PrendaCelda` (props aditivas), `--color-papel` en `globals.css` (todo el sistema). **No toca `crear_devolucion`/`aprobar_devolucion`** (la migración `20260918163712` de `claude/blissful-mccarthy-3b06e5` es de otra sesión). Commits locales, sin push. Falta: clic real con datos reales. | 2026-09-18 |
| Claude (worktree `product-creation-decision-tree-0afe63`) | `claude/product-creation-decision-tree-0afe63` | Crear producto como árbol de decisión + marca y proveedor (ADR-0109): trigger e índice de nombre sobre `productos`, `buscar_productos_parecidos`, curva habitual, mapa categoría→atributos, `marcas` + `marca_proveedores`, `productos.marca_id/proveedor_id` (NOT NULL, llave compuesta), cambia `crear_producto_con_variantes`, `censo_crear_variante`, `catalogo_actualizar_producto`, `fn_productos`, `fn_productos_resumen`, `fn_productos_buscar`; retira `catalogo_crear_producto`. Pantallas: Nuevo producto, Editar producto, censo (alta al vuelo), Productos (filtros, tarjeta, «A quién pedirle»), Catálogo → Marcas, Categorías (curva). Migraciones `20260918230000`–`20260918231300` (fuera de la banda de Compras). Falta pegar los 8 SQL en producción, EN ORDEN y ANTES de desplegar, y verificar con sesión de Líder real. | 2026-09-18 |
| Claude (worktree `buscar-entry-point-7aa994`) | `claude/local-work-3a718a` | Punto de Venta (ADR-0114): comprobante impreso en térmica de 80 mm + modal «Venta registrada» con información útil (`VentaRegistradaModal.tsx`, `ReciboTermico.tsx`, `lib/recibo-reglas.ts`, `lib/emisor.ts`), modal de talla (`ElegirTallaModal.tsx`), «Solo con stock» activo por defecto, avisos de tope de stock, un color por método de pago (**cambia los tokens `--color-metodo-*` de `globals.css`, que también usa la dona de Caja**), quitar método con traspaso de monto, subtotal/IGV en el pie. Toca `PuntoDeVenta.tsx`, `PuntoDeVentaCatalogo.tsx`, `PuntoDeVentaTicket.tsx`, `vender-reglas.ts`, `globals.css`. Sin migración. Sin commitear todavía. | 2026-09-18 |
| Claude (worktree `proveedores-ui-ux-animaciones-48011f`) | `claude/proveedores-ui-ux-animaciones-48011f` | Proveedores: vista rápida, mini-tendencias, FLIP/pulgar/destello/deshacer (ADR-0128). Toca `ProveedoresPanel.tsx`, `ProveedorModal.tsx`, `ProveedorCostoEvolucion.tsx`, `ui/Avisos.tsx` (acción «Deshacer»), `lib/proveedores*.ts`, `compras/proveedores/**`, `globals.css` (bloque «Proveedores», clases nuevas), migración `20260919150000`. Sin cambios de esquema más allá de esa función. | 2026-09-19 |
| Claude (worktree `produccion-compras-avios-9d6d34`) | `claude/produccion-f4a-proveedores` | **Producción como módulo propio** (ADR-0133, plan en `docs/PLAN-PRODUCCION.md`): módulo **aparte de Compras** (que no se toca), con su propio abastecimiento a futuro (proveedores, comprobantes, por pagar, recibir de tela y avíos), Órdenes, Insumos, Resumen y Eficiencia. **F0–F3b hechas, F4a en curso (`claude/produccion-f4a-proveedores`)** (F3b: devolver insumos, migración `20260920100000` sin pegar en producción) (menú, Órdenes como tablero, Insumos: `AppShell.tsx`, `lib/produccion*.ts`, `lib/insumos*.ts`, `lib/useFlipCajas.ts`, `components/Orden*.tsx`, `Insumo*.tsx`, `MatrizOrden.tsx`, `app/(app)/produccion/**`). Sigue F4 (esquema nuevo propio: `proveedores_produccion`, `comprobantes_produccion`…; **migraciones ≥ `20260919210000`**). **No toca** `compras`, `compra_items`, `registrar_compra` ni `recibir_compras` (D-H): sin dependencia con el reparto entre tiendas (ADR-0139). | 2026-09-19 |
| Claude (sesión web `gracious-ride-cc6pic`) | `claude/gracious-ride-cc6pic` | **Rediseño visual de Inventario con la paleta oficial CAYLA Dynamic** (Existencias, Movimientos, Traslados, Conteo, Análisis): tokens de `globals.css` (papel, verde, ámbar, taupe, hueso, pizarra), `ui/Tabla`, `ui/TarjetaCifra`, `ui/Chip`, encabezado de página. Solo visual: sin migraciones. Ventas espera a que se fusionen sus ramas en curso. | 2026-09-22 |

## Cerradas hoy

| Quién | Rama / worktree | Qué tocó | Cerrada |
| Claude (worktree `activity-combo-box-style-819a5d`) | `claude/activity-combo-box-style-819a5d` ([PR #475](https://github.com/felipea92p-ux/cayla-retail/pull/475), abierto) | **Un solo combo en todo el ERP (ADR-0209 act. 2026-09-26)**: el combo de Actividad, y después ningún `<select>` del navegador en la web, Finanzas incluida. **Aviso a quien toque combos o Finanzas:** cambió `components/ui/campos.tsx` (`Desplegable`: `Opcion.grupo`/`deshabilitada`, `id`, `ref`, `className`, formas `fin`/`finEnLinea`; la forma `caja` pasa a hueso; se borraron `SelectNativo`/`CampoSelectNativo`), `components/finanzas/kit.tsx` (`SelectFin` ahora es `valor`/`onValor`/`opciones`, sin `<option>`; nuevo `SelectEnLineaFin`), `finanzas.css` (`select.fin-control` → `.fin-desplegable`/`.fin-desplegable-caja`), `CampoCuenta.tsx` (sin `OpcionesCuenta`: usa `opcionesDeCuenta` de `lib/cuenta-sellada-reglas.ts`) y los 18 archivos de Finanzas que usan `SelectFin`, más Devoluciones pendientes, Medios de pago, Apartados, `DecisionFaltanteFila.tsx`, `CambioReemplazo.tsx`/`CambiosFlujo.tsx` y `ui/Modal.tsx` (sin `campoSelect`). Prueba nueva `lib/sin-select-nativo.test.ts`: **un `<select>` nuevo rompe el CI**. Sin migración. | 2026-09-26 |
| Claude (worktree `edicion-marcas-280a6c`) | `claude/gasto-proveedor-parecido` (PR #452; el #450, del que salía, ya está en `main`) | **«¿No será un proveedor que ya tienes?» en Gastos** (ADR-0109 act. (c)). **Aviso a Finanzas: tocó `RegistrarGastoModal.tsx`** (solo el bloque «¿No está? Súmalo»: estado `descartados`, `sumarProveedor`, la caja `PreguntaParecido`) y agregó `sumarProveedorDeGasto` al final de la sección del formulario de `lib/gastos-reglas.ts` (+ import de `nombres-parecidos`/`proveedores-reglas`). Prueba nueva `lib/gastos-proveedor-parecido.test.ts`. Sin migración. La fila de F2b de arriba parece vieja: F2b y F3b ya están en `main` y la rama `claude/finanzas-f2b-activos` no existe. | 2026-09-25 |
| Claude (worktree `cayla-retail-documentation-f1ddf0`) | `claude/search-filter-brand-edit-ae6183` | **Catálogo ▸ Marcas: buscador y «Editar»** (captura de Felipe). Migración `20260926150000_editar_marca.sql` (solo crea `retail.editar_marca`, SIN pegar en producción) + `scripts/pruebas/editar_marca.mjs` (CI). Web: `MarcasLista.tsx`, `EditarMarcaModal.tsx` (nuevo), `productos/marcas/page.tsx`, `lib/marcas.ts`, `packages/database/src/types.ts`. **Llama a `registrar_proveedor` por nombre de parámetro** (`p_nombre`, `p_ruc`): la rama de varios rubros la mantiene compatible. | 2026-09-25 |
| Claude (worktree `stock-realtime-sync-879d3c`) | `claude/stock-realtime-sync-879d3c` | **Stock en vivo por sondeo: Vender + Apartados + Cambios (ADR-0018, «Actualización 2026-09-25»)** — reporte de Felipe (Vender) más su pedido de auditar todo el sistema. Auditoría de ~30 módulos: casi todo ya tiene candado de servidor (`for update`), el único hueco real de "pantalla vieja" estaba en Apartados/Cambios además de Vender. Solo web, sin migración: `lib/useStockEnVivo.ts` (nuevo, hook compartido) + `PuntoDeVenta.tsx` (refactor para usarlo), `components/apartados/ApartarVista.tsx`, `components/CambiosFlujo.tsx`. También revisó (sin tocar código: la función no existe todavía) un candado pendiente en la ficha de clienta — nota en BACKLOG. `tsc`/`eslint`/76.992 pruebas en verde; verificado contra el Postgres local por PostgREST. **No toca** `vender-stock-local.ts` ni ninguna RPC — quien lo lea antes de mí puede seguir de largo. | 2026-09-25 |
| Claude (worktree `pos-ticket-seller-selection-95d5b3`) | `claude/pos-ticket-seller-selection-95d5b3` | **«Quién vendió» en el ticket del Punto de venta** (ADR-0163), rehecho el 2026-09-22 sobre `ventas.asesora_id` + `fn_asesoras_de_turno` de main (ADR-0153): la fila «Atendió» lee la asistencia de Dynamic cada minuto. Migración propia: solo `20260922213700` (`fn_ventas_del_dia`), sin pegar. Web: `PuntoDeVenta.tsx`, `PuntoDeVentaTicket.tsx`, `VendedorasFila.tsx`, `lib/useVendedorasDeTurno.ts`, `lib/vender-reglas.ts`, `lib/ventas-historial*.ts`, `lib/ventas-v2.ts`, `lib/ventas-offline.ts`, `app/actions/caja.ts`, `packages/database/src/types.ts`. Siguiente: nota de venta. | 2026-09-22 |
| Claude (worktree `brave-northcutt-7a1d2c`) | `claude/apartado-stock-reserva-fisica` | **Apartar stock, Fase 1** (ADR-0141): `stock.cantidad_apartada`, tabla `apartados`, RPC `apartar_stock`/`liberar_apartado`/`listar_apartados`, y **reescribe `fn_aplicar_movimiento` y `recalcular_stock`** (quien las toque: partir del cuerpo de esa migración, no de una copia vieja). Migración `20260920160000`. Web: `InventarioPanel.tsx` (Existencias), `ApartarModal.tsx`/`ApartadosModal.tsx`, `lib/inventario-v2.ts`, `lib/movimientos-reglas.ts`, y una línea en `vender/page.tsx`, `cambios/page.tsx` y `inventario/mover/page.tsx`. **No toca** `registrar_venta`, Caja ni Finanzas (la Fase 2, el adelanto, sí lo hará y se anota aquí antes). ADR-0141. | 2026-09-20 (PR #209 fusionado) |
| Claude (worktree `charming-wright-f5c166`) | `claude/charming-wright-f5c166` | «Ajustar inventario» se abría vacío: `AjustarInventarioModal.tsx` pedía `variantes.talla` (eliminada por la taxonomía cerrada, ADR-0095 / `20260917100500`). Ahora usa `talla:tallas ( valor )` sin `as unknown as` (si vuelve a pedir una columna inexistente, `tsc` falla), y arma las filas en `lib/ajuste-reglas.ts` con las tallas en orden de curva (12 pruebas). Barrido de `apps/web`: ningún otro select ni filtro pide `talla` a secas sobre `variantes`. Sin migración ni esquema; no tocó `fn_aplicar_movimiento`, `recalcular_stock` ni «apartar stock» (ADR-0141). PR [#210](https://github.com/felipea92p-ux/cayla-retail/pull/210) abierto, esperando revisión. Falta verlo en navegador con datos reales. | 2026-09-20 |
| Claude (worktree `modulos-por-tienda-ca0f59`) | `claude/modulos-por-tienda-ca0f59` | **Reparto de un comprobante de compra entre tiendas (ADR-0139; antes 0107, 0132 y 0138).** **Fusionado (PR #203) y aplicado en producción el 2026-09-20** (migraciones `20260919172000` y `20260919173000` pegadas por Felipe y verificadas en solo lectura; volcado y diccionario refrescados). Toca: base (`compra_item_destinos`, `compra_reasignaciones`, `compra_item_reparto_resumen`, `fn_puede_ver_compra`, `reasignar_reparto_compra`; `recibir_compras`/`recibir_envio`/`registrar_compra`/`cerrar_linea_compra`/lecturas operativas; `compras.ubicacion_destino_id` ELIMINADA); web: Registrar (`CompraFormV2` + `RepartoEnRegistro`), `/recibir` por tienda, detalle (`CompraDetalle`, `RepartoPorTienda`, `ReasignarReparto`, `CerrarFaltanteModal`), lista de `/compras`; `lib/reparto-reglas.ts`, `lib/compras-reparto.ts`; aviario (Pelícano). También ajustó 3 suites de otras sesiones que daban por existente la columna eliminada y corrigió 6 referencias «ADR-0132» de otras sesiones que querían decir el reparto. Quien toque `compras`/`compra_items`/`registrar_compra`/`recibir_compras`: partir de `main` (esta rama ya está fusionada). El filtro «Destino» que faltaba se hizo el 2026-09-21 (fila `claude/filtro-destino-compras`). CERRADA. | 2026-09-21 |
| Claude (worktree `modulos-por-tienda-ca0f59`) | `docs/refresco-completo-volcado` | Refresco COMPLETO de `docs/datos/generado/` (los 7 archivos contra producción, en solo lectura, verificado por hash) + 4 tablas de Producción asignadas al Gallito en `scripts/datos/aviario.mjs`. Solo documentación y esa línea del aviario; sin migraciones ni código de la web. **Fusionado (PR #217).** CERRADA. | 2026-09-21 |
| Claude (worktree `modulos-por-tienda-ca0f59`) | `claude/filtro-destino-compras` | Filtro «Destino» en Comprobantes y Por pagar (anexo del ADR-0139): migración `20260921130000` (`p_ubicacion_id` en `listar_compras` y `por_pagar_tramos`), `lib/compras.ts`, `lib/compras-reglas.ts`, `lib/compras-indicadores.ts` (solo `getPorPagarTramos`), `components/FiltrosCompras.tsx`, `compras/page.tsx` y `compras/por-pagar/page.tsx` (una línea de filtros y la carga de tiendas), y sus pruebas. **No toca** Producción ni Recibir. **Fusionado (PR #221) y migración aplicada en producción.** CERRADA. | 2026-09-21 |
|---|---|---|---|
| Claude (worktree `inventory-view-ux-analysis-ca30fa`) | `claude/resumen-comparacion-periodos` | Análisis de inventario (antes Resumen): Desempeño + Comparar períodos A vs B (ADR-0138) con rediseño visual (dona «Evolución del ritmo», barras de rotación, distribución de sell-through, sin cobertura ni interpretación de texto), Rotación centralizada (`lib/rotacion.ts`, total sobre el universo confiable) y miniatura + color como Existencias en Desempeño/Comparar/Movimientos/Traslados/Conteo. Migración `20260919220000` (`fn_resumen_comparacion`) **aplicada en producción** (ensayo revertido → `apply_migration` → verificada por catálogo y `md5`). Fusionada con `main` (80 commits; un conflicto real, en esta BITÁCORA, resuelto conservando las dos mitades) y con PR fusionado a `main`. | 2026-09-19 |
| Claude (worktree `proveedores-ui-ux-animaciones-48011f`) | `claude/recibir-diseno-fiel-al-spike` | Recibir mercadería: igualar el diseño y las animaciones de `/recibir` al spike (ADR-0129, ampliación). Toca `RecepcionEnvio.tsx`, `DecisionFaltanteFila.tsx`, `ResumenPrevioEnvio.tsx`, `RecepcionesCompraLista.tsx`, `FiltrosRecibidas.tsx`, `ui/BarraFija.tsx`, `KpisRecibir.tsx`, `recibir/page.tsx`, `globals.css`. Sin migraciones. Quien vaya a tocar `/recibir` (p. ej. «Módulos por tienda»): fusionar esto primero. | 2026-09-19 |
| Claude (worktree `receiving-module-design-3f2904`) | `claude/por-pagar-ui-animations-4ffe60` | Por pagar responde (ADR-0131, aplicó el spike `docs/maquetas/por-pagar-spike-2026-09/`, mismo modelo que ADR-0128): entrada escalonada, cifras que cuentan, vencimiento/caja/concentración que se encienden entre sí y filtran, «¿alcanza la caja?», vista rápida del comprobante, barra fija con «＋ agregar», cascada del pago y reacción de la pantalla al pagar. Toca `PorPagarLista.tsx`, `PagoJuntosModal.tsx`, `DeudaPorVencimiento.tsx`, `SalidasDeCaja.tsx`, `SaldosAFavor.tsx`, `app/(app)/compras/por-pagar/page.tsx`, `lib/por-pagar-reglas.ts` (+ componentes nuevos `PorPagar*`) y añade clases a `globals.css`. Más: Pagar juntos con varios medios (ADR-0132) — **migración nueva `20260919190000_pago_por_lote_varios_medios.sql`** (función `registrar_pago_compras_medios`), toca también `PagoPiezas.tsx`. | 2026-09-19 |
| Claude (worktree `proveedores-ui-ux-animaciones-48011f`, rama `claude/recibir-mercaderia-ui-ux-936cd2`) | `claude/recibir-mercaderia-ui-ux-936cd2` | Recibir mercadería: spike visual aplicado a `/recibir` (ADR-0129): `RecepcionEnvio`, `KpisRecibir`, `RecepcionesCompraLista`, componentes nuevos (`ResumenPrevioEnvio`, `EnvioRecibido`, `RecepcionVistaRapida`, `TabsSubrayado`), `lib/envio-reglas.ts`. Sin migración. | 2026-09-19 |

| Claude (worktree `inventory-view-ux-analysis-ca30fa`) | `claude/inventory-view-ux-analysis-ca30fa` | Resumen de Inventario v2 (`/inventario/resumen`, ADR-0121): período elegible + comparación, velocidad por días con stock, motor de reposición, curvas rotas, búsqueda por tokens, capital con verificación de costo. Migración `20260919141804` (antes `20260919010000`, choque con `etiquetar_variantes`; misma `fn_resumen_variantes`, firma nueva, la vieja se elimina), `resumen-*.ts` en `lib/`, componentes `Resumen*.tsx`, `ui/TarjetaSenal.tsx`, `ui/Graficos.tsx` (barras + dona), `SelectorUbicacion.tsx`, `ReponerPisoModal.tsx`. `lint`/`typecheck`/`build`/637 pruebas + 38 verificaciones SQL en verde; visto en navegador a 1440/1280/~1024/móvil. **Migración `20260919141804` ya aplicada en producción (2026-09-19, con autorización de Felipe, verificada)**; el frontend sale con el merge del PR #161 (la pantalla vieja seguía funcionando entre medias). No tocó Existencias, Traslados, Caja, Catálogo ni `catalogo_actualizar_producto`. | 2026-09-18 |
| Claude (worktree `inventory-view-ux-analysis-ca30fa`) | `claude/movimientos-simplificados` | Movimientos simplificado (PR #179, ADR-0127): columna «Referencia» (Traslado N / Conteo N con enlace, comprobante, factura, guía), buscador por proceso, filtros a la vista reducidos, sin Persona/Responsable. Tocó `movimientos/page.tsx`, `FiltrosMovimientos.tsx`, `MovimientosLista.tsx`, `MovimientoDetalle.tsx`, `lib/movimientos-{reglas,v2}.ts`, `lib/traslados-reglas.ts`, `ui/Tabla.tsx` (prop aditiva), `fn_movimientos`/`fn_movimientos_resumen` + 2 funciones nuevas (migración `20260919155000`, **aplicada en producción**), `scripts/pruebas/fn_movimientos_referencias.mjs` y un paso en `ci.yml`. | 2026-09-19 |
| Claude (worktree `angry-archimedes-c8065a`) | `claude/peaceful-swanson-5f6f73` | Clases de componente de `globals.css` (`.card-cayla`, `.label-cayla`, `.font-display`, `.alza-cayla`, `.scroll-cayla`, `.anim-*`) movidas a `@layer components` (ADR-0105): el acento `border-l-2 border-l-rojo` de las tarjetas por fin se dibuja. Tocó solo el bloque de clases de componente de `globals.css` (no los tokens `--color-metodo-*`), 21 `className` con utilidades muertas en 10 archivos (incluye `CajaAbiertaPanel`/`CambiosLista` de la sesión de Ventas: solo se quitó un `font-bold`/`font-semibold` por línea), `TarjetaCifra`, las tarjetas locales de Conteo/Traslados y el banner de altas pendientes de `productos/page.tsx` (enlaces de acción de rojo a tinta subrayado, decidido por Felipe), y `lib/globals-capas.test.ts` (nuevo: falla si vuelve una clase suelta). **Para `comercial-command-leaders-bb5771`:** `ComercialPanel.tsx` ya no necesita `border-l-2! border-l-rojo!` — quita los `!`. Verificado con el componente real y 752 `className`; sin datos reales (Docker local caído, no se reinició). `tsc`/`eslint`/419 tests en verde. PR pendiente de abrir. | 2026-09-18 |
| Felipe (Claude) | `claude/tejidos-seed` (worktree `fervent-jackson-88b6f2`) | Sembró 17 tejidos (Algodón pima, Alpaca, Algodón, Denim, Drill, Lino, Seda, Viscosa, Poliéster, Popelina, Gabardina, Pana, Polar, Licra, Jersey, Piqué, Rib) — vocabulario vacío desde ADR-0095. Investigado contra Google Merchant Center + proveedores de Gamarra, decisiones ya negociadas con Felipe en sesión previa (nombres únicos sin "/"). Migración `20260918140000`. `db reset`/typecheck/lint/navegador en verde. PR pendiente de abrir. | 2026-09-18 |
| Claude (Danytristee) | `claude/rediseno-visual-caja-y-vender` | Rediseño visual de Caja (tablero completo, ADR-0101) + misma piel visual extendida a Punto de Venta (chips a `rounded-md`, selector de método de pago con los 3 colores categóricos de la dona). Sin lógica nueva, sin tocar `vender-reglas.ts`/`ventas-offline.ts`. Migración nueva `ubicaciones.meta_venta_diaria` (nullable), aplicada en local, pendiente producción. `tsc`/`lint` en verde, verificado en navegador con datos reales de Tienda Lima. | 2026-09-18 |
| Claude (worktree `ganso-module-mockup-4f4def`) | `claude/analisis-local-categorias-be96a0` | Rediseño visual de `/productos/categorias` (tarjetas + "Vista rápida") y consolidación de Colores/Tallas/Tejidos/Patrones/Etiquetas en `/productos/atributos` (pestañas + modales) — `CategoriasLista.tsx`, `AtributosHub.tsx`, `AppShell.tsx`, `next.config.ts` (5 redirects). Fusionado con `main`: reconcilió el cambio paralelo de Etiquetas (estilo visual + vigencia, sin sedes) sin perder ninguna de las dos features. `tsc`/`lint`/297 tests en verde. PR [#119](https://github.com/felipea92p-ux/cayla-retail/pull/119) abierto, esperando revisión de Felipe. | 2026-09-18 |

| Felipe (Claude) | `claude/supplier-metrics-analysis-87a863` (worktree `cayla-invoices-module-review-451aa5`) | Proveedores: ficha ampliada (`rubro`/`plazo_credito_dias`/`forma_pago_preferida`), métricas de compras/insumos por sección solo-líder (corrección angosta de D-27), cierre de `devolver_proveedor` (ADR-0094, sin choque). Fusionado con `main` (26 commits) — dos sobrecargas duplicadas reales encontradas y cerradas en el camino (`resolver_prenda_danada`, y el propio `aprobar_devolucion`/`resolver_prenda_danada` reconstruidos sobre la versión real de Nota de Crédito automática y de "Liquidada exige venta"). Migraciones renombradas a `20260918070000`-`20260918073000` por choque de timestamp con 3 archivos de otras sesiones. `db reset`, typecheck, lint y 297 tests en verde sobre el árbol ya mezclado. PR [#117](https://github.com/felipea92p-ux/cayla-retail/pull/117) abierto, esperando merge desde GitHub. | 2026-09-18 |

| Quién | Rama / worktree | Qué tocó | Cerrada |
|---|---|---|---|
| Claude (Danytristee) | `claude/redesign-cierre-caja-grafico-c74e92` (worktree `venta-caja-screens-animations-7923b9`) | Caja abierta: rediseño por el ancho del propio tablero (`@container`, todo el ancho, modales fuera del contenedor) + actualización en vivo por sondeo cada 5 s (`useCajaEnVivo`; sin Realtime, que sería un DDL en el proyecto compartido con Dynamic, ADR-0018) con animación de venta nueva; dona propia en SVG y "Ritmo del día" en vez de ventas por hora. Toca `CajaAbiertaPanel.tsx`, `ui/Graficos.tsx`, `caja-panel-reglas.ts`, `AppShell.tsx` (`SIN_TOPE_DE_ANCHO`), `globals.css`, `caja/page.tsx` y `caja/historial/page.tsx`; nuevos `ui/DonaMetodos.tsx`, `caja-en-vivo.ts`, `useCajaEnVivo.ts`, `useNovedades.ts`, `useEnVista.ts`, `dona-geometria.ts`. Sin esquema ni RPC. **ADR: el de Caja pasa de 0102 a 0116** (choque con `0102-emitir-comprobante…`); 0112 a 0114 ya los reclaman otras ramas en vuelo (el 0113 tres) y se deja el 0115 libre para quien tenga que moverse. Fusionada dos veces con `main`; la segunda dio conflicto en `globals.css` y `CajaAbiertaPanel.tsx` por #153 (ADR-0105: las clases nuevas pasan a `@layer components`). | 2026-09-18 |
| Claude (Danytristee) | `claude/blissful-mccarthy-3b06e5` | **Producción ↔ repo: se trae al repo lo que corría en producción sin archivo.** Migraciones reconstruidas e idempotentes (anulación de ventas con su `cerrar_caja`, historial de producto, `registrar_movimiento` y `catalogo_actualizar_producto` a una sola firma, `TRUNCATE` de `movimientos`, tejidos/patrones, y `compras.token_cliente` con `registrar_compra(..., p_token)`), más `pnpm pruebas:deriva-produccion`. `CompraFormV2` ahora manda `p_token`. `fn_tallas_estado_trigger` se aplicó en producción (única escritura allá, con la autorización de Felipe). **`aprobar_devolucion` y `crear_devolucion` NO cambiaron:** `aprobar_devolucion` ya se redefinió 6 veces; si vas a tocarla, parte del cuerpo de `pg_proc.prosrc` de producción, no del archivo más viejo. Tu Postgres local compartido ya las tiene aplicadas. Pendiente: `gastos`/`registrar_gasto` (sin migración numerada) y lo de la rama de Cambios. | 2026-09-18 |
| Felipe (Claude) | `docs/refresca-volcado-produccion-2026-09-18` | Refresca el volcado de producción de `docs/datos/generado/` (7 archivos, solo lectura contra `cayla-dynamic`): entra `familias`, aviario en verde con 61 tablas, `datos:comparar` sin roto. Hallazgo: `imagen_muestra_url` de patrones/tejidos vive en producción sin migración en `main`. | 2026-09-18 |
| Felipe (Claude) | `claude/aviario-cayla-8d1efc` | Aviario (ADR-0104, renumerado desde 0103 por el de familias): una sola lista tabla→pájaro en `scripts/datos/aviario.mjs` (nuevo), índice generado `docs/datos/generado/AVIARIO.md`, paso "Aviario" en `.github/workflows/ci.yml`, `scripts/datos/generar.mjs`, §1/§4/§8 de `docs/datos/07-GOBIERNO.md`, `COMO-REFRESCAR.md` y el diccionario regenerado (solo títulos y orden). Sin tablas, funciones ni pantallas. Asignaciones aprobadas por Felipe; PR [#136](https://github.com/felipea92p-ux/cayla-retail/pull/136) abierto, esperando merge. | 2026-09-18 |
| Felipe (Claude) | `claude/arreglemos-esto-079019` | **ADR-0103 — colisión resuelta con Felipe en vivo:** esta sesión y `claude/fix-old-stuff-0192ff` construyeron en paralelo "familia como tabla propia" (misma tarea de ADR-0096/BACKLOG), cada una con un diseño distinto. Felipe comparó las dos y eligió la de acá (`familias.codigo` texto, sin proponer/aprobar — mismo patrón que Categorías). **`fix-old-stuff-0192ff`: por favor descarta tu versión de Familias** (`retail.familias` con `id uuid`/proponer-aprobar, `categorias.familia_id`) — ver ADR-0103 para el porqué completo. Migraciones/API/pantalla de Colores agrupada por familia + 4 tonos nuevos (Cobalto/Gris antracita/Caqui/Tostado, investigación real Zara/Ralph Lauren/Platanitos/LVMH) son independientes, no hace falta descartarlas. `pnpm typecheck`/`lint`/297 tests en verde, verificado en navegador como líder. **Reconciliado con `main` (sesión `reorder-colors-5bbc2c`):** 7 conflictos resueltos, ADR renumerado a 0103, migración de colores a `20260918154730`, y arreglado un error de tipos propio del PR. | 2026-09-18 |
| Felipe (Claude) | `claude/main-branch-sync-workflow-a1b6ba` (worktree `erp-architecture-summary-1c70d9`) | "Resumen" de Inventario por variante × sede contra la referencia visual (ADR-0101, renumerado desde 0097 por choque con `activar-tienda-lima-eff087`): RPC `fn_resumen_variantes`, `lib/resumen-reglas.ts`, `ui/TarjetaCifra`+`ui/PrendaCelda`, prellenado de `/inventario/mover`. Fusionada con `main` (22 commits, PR #106-#119): reconcilió `conteo/page.tsx` con el alta al vuelo del PR #108 sin perder ninguna de las dos features, y de paso corrigió `fn_prioridad_conteo` (devolvía una fila por variante×sububicación sin decir cuál era cuál). **PR #120 mergeado a `main` y las dos migraciones ya en producción**, ambas con ok explícito de Felipe — ver BITACORA 2026-09-18. Pendiente sin mergear: PR #121 (refresco de `funciones-produccion.txt`, docs-only). | 2026-09-18 |
| Felipe (Claude) | `claude/etiquetas-ajustes-taller-galentine` | "Para liquidar" de 4 filas a 1 global (PR [#115](https://github.com/felipea92p-ux/cayla-retail/pull/115), fusionado) + vigencia conectada a `ProductoForm.tsx` + estilo visual (3 colores fijos, sin rojo por `MAX_ROJO_POR_PANTALLA`, migración `20260918060000`) + pantalla agrupada por color en vez de grilla plana. `db reset` completo + navegador verificados. | 2026-09-18 |
| Felipe (Claude) | `worktree-taxonomia-cerrada-continuacion` | Fix producción: `notas` faltaba en `retail.tejidos`/`patrones`/`etiquetas`. Censo alta al vuelo (ADR-0099), PDF/XML/CDR de SUNAT, y Nota de Crédito automática en devoluciones (ADR-0100) — PR #108 fusionado por Felipe, ambas migraciones (`20260918020000`/`20260918050000`) ya aplicadas y verificadas en producción. Pendiente real de Felipe: registrar serie `nota_credito` por ubicación. | 2026-09-18 |
| Felipe (Claude) | `claude/tags-section-5db96b` | `retail.etiquetas` sin `notas` — Catálogo > Etiquetas caía en vivo. Diagnóstico por lectura read-only, fix aplicado por Felipe en SQL Editor, trigger local reconciliado, diccionario de `docs/datos/` refrescado completo (45→60 tablas, faltaba toda la taxonomía de ADR-0095). PR [#107](https://github.com/felipea92p-ux/cayla-retail/pull/107) fusionado. | 2026-09-17 |
| Felipe (Claude) | `claude/etiquetas-vocabulario-comercial` | Vocabulario real de Etiquetas: 22 filas sembradas (comerciales/festividades/Para liquidar por sede), `vigente_desde`/`vigente_hasta`, comentario obligatorio al aprobar en las 5 vocabularios. Verificado con `db reset` completo + navegador. PR [#113](https://github.com/felipea92p-ux/cayla-retail/pull/113) aparte del #107, a pedido de Felipe. UI de vigencia/estilo visual queda pendiente en BACKLOG a propósito. | 2026-09-17 |
| Claude (worktree `activar-tienda-lima-eff087`) | `claude/activar-tienda-lima-v2` | Alta de Tienda Lima en producción: `retail.ubicaciones` + `retail.sububicaciones` (ADR-0097, migración `20260918010733_activar_tienda_lima.sql`), verificada con dry-run (rollback) y aplicada con ok de Felipe | 2026-09-18 |
| Felipe (Claude) | `claude/validar-tareas-sistema-89d271` | Tarea 1: cola offline en `CerrarCajaModalV2.tsx`/`CajaAbiertaPanel.tsx`/`PuntoDeVenta.tsx` (ADR-0092) | 2026-09-17 |
| Felipe (Claude) | `claude/validar-tareas-sistema-89d271` | Tarea 2: inventario de insumos del Taller — esquema huérfano adoptado + `registrar_consumo_insumo` (ADR-0090, reconciliado con `claude/strange-golick-420bb9`) | 2026-09-17 |
| Felipe (Claude) | `claude/validar-tareas-sistema-89d271` | Tarea 3: liberar comprobante SUNAT pendiente (ADR-0093) | 2026-09-17 |
| Felipe (Claude) | `claude/validar-tareas-sistema-89d271` | Tarea 4: exportar CSV en `InventarioPanel.tsx` | 2026-09-17 |
| Felipe (Claude) | `claude/validar-tareas-sistema-89d271` | Tarea 5: prueba `scripts/pruebas/fn_aplicar_movimiento.mjs` | 2026-09-17 |
| Felipe (Claude) | `claude/validar-tareas-sistema-89d271` | Tarea 6: ADR de la unificación retail↔dynamic (ADR-0091) | 2026-09-17 |
| Felipe (Claude) | `claude/validar-tareas-sistema-89d271` | Fusión con `main` (48 commits) + renumeración de ADR 0074-0077→0078-0081 (colisión con 3 sesiones concurrentes) + ejecución en producción de `registrar_consumo_insumo` y `marcar_comprobante_no_emitido`, con ok explícito de Felipe (excepción puntual a D-11). Segunda fusión: renumerado otra vez 0078-0081→0090-0093 (nueva colisión con `hopeful-knuth-b7e000`, ADR-0078 revocar-execute). Tercera fusión: solo docs + `package.json` (pruebas de `registrar_venta`, sin choque de ADR esta vez). PR #96 abierto, esperando merge desde GitHub. | 2026-09-17 |
| Claude | `claude/pruebas-registrar-venta-cd2119` | Pruebas: `registrar_venta` (`scripts/pruebas/registrar_venta.mjs`, 22/22) + job piloto de CI con Postgres real (ADR-0094, `.github/workflows/ci.yml`, sin pushear todavía) | 2026-09-17 |
| Claude Code | `claude/almacen-redirects-config-8733a3` | `next.config.ts` (redirects de `/almacen`), borró `app/(app)/almacen/**`, `docs/ARQUITECTURA.md` (línea del alias) | 2026-09-17 |
| Claude (Danytristee) | `claude/stock-fantasma-archivados-239a85` | Stock fantasma de productos de prueba — ya estaba resuelto en producción sin script (2026-09-16 21:44 UTC); agregó filtro defensivo en `inventario-v2.ts` | 2026-09-17 |
| Felipe | `claude/buscar-entry-point-7aa994` | Punto de entrada a `/buscar`: tarjeta en Acciones de Inicio + campo propio (`buscar/page.tsx`, `page.tsx`, BACKLOG, BITACORA) | 2026-09-17 |
| Felipe (sesión Claude) | `claude/supabase-unificacion-audit-0887f5` | ADR-0063 (venta sin red): intenté cerrar el hueco de verificación del camino feliz. **Bloqueado, no de esta sesión ni de ADR-0063**: mi Postgres local compartido tiene `retail.variantes.talla_id` (FK a una tabla `retail.tallas` con flujo proponer/aprobar, `created_at` de HOY 19:48 UTC) que **no existe en ningún migration file de `main`** — rompe `getCatalogo()` (`talla` ya no existe como columna) y por lo tanto toda la pantalla `/vender`. No toqué el esquema: parece un cambio de otra sesión en curso sobre el mismo Postgres compartido, aplicado directo sin migración todavía. Ver aviso a Felipe. | 2026-09-17 |
| Claude (sesión Danytristee) | `claude/compras-rls-location-lock-7a8b0c` (worktree `cuervo-colibri-modulos-170de6`) | RLS de `retail.compras`/`compra_items`/`compra_pagos`/`compra_adjuntos` + `resumen_compras()` acotados a `fn_puede_operar_ubicacion` (ADR-0075, decidido con Felipe vía `/decide`). Aplicado y verificado en local; pendiente producción con ok de Felipe. No tocó `compras_multipago.sql`/`anular_compra`/`movimientos` RLS/`ventas-v2.ts`/`movimientos-reglas.ts`/`fn_prioridad_conteo`. | 2026-09-17 |
| Claude (worktree `mejorar-modulo-por-pagar-48008c`) | `claude/mejorar-modulo-por-pagar-48008c` | UX de `/compras/por-pagar` en celular (tarjetas de resumen + `FiltrosCompras.tsx` compartido) — ver worktree `payable-module-review-db60ea`, sin commits/cambios | 2026-09-17 |
| Claude (worktree `productos-fuera-factura-f85f65`) | `claude/productos-fuera-factura-f85f65` | `recibir_compras` acepta productos fuera de factura (ADR-0076 — corregido acá, la fila decía 0075 por la colisión que la propia sesión resolvió) — ya en producción; PR #78 abierto, esperando que Felipe le dé merge desde GitHub (el entorno bloquea el merge desde el chat). Reconciliado con `main` 3 veces por sesiones concurrentes, sin perder contenido de nadie. | 2026-09-17 |
| Claude (worktree `redesign-venta-module-801c9b`) | `claude/catalog-clothing-nomenclature-design-2efcd0` | Vista de grilla de Productos con swatches de color, filtros rediseñados, orden por precio y fotos por color (ADR-0077 — renumerado de 0075 por colisión con `0075-compras-lectura-acotada-por-sede.md`, ya en `main`). PR #81 mergeado a `main`. De paso, detecté 2 pares de migraciones con timestamp duplicado ya mergeados (`20260917100000` y `20260917140000`, de otras 2 sesiones) — rompen `migration up --local` para cualquier worktree; aplicadas acá con workaround local, falta que alguien renombre un archivo de cada par. | 2026-09-17 |
| Claude (worktree `hopeful-knuth-b7e000`) | `claude/hopeful-knuth-b7e000` | Revoke de EXECUTE público en `fn_aplicar_movimiento`/`fn_reservar_numero_serie`/`fn_siguiente_correlativo`/`fn_asignar_codigo_producto`/`fn_asignar_codigo_variante` (ADR-0078 — renumerado dos veces el mismo día, primero desde 0074 por `prioridad-de-conteo-por-valor`, después desde 0077 por la fila de arriba). Ya en producción, verificado con `has_function_privilege`. No tocó ninguna tabla ni RPC de negocio, solo `GRANT`/`REVOKE`. PR #93 abierto contra `main`. | 2026-09-17 |
| Claude (worktree `recordatorio-lectura-artefacto-4de334`) | `claude/recordatorio-lectura-artefacto-4de334` | Verificación en navegador del rediseño de Compras (2026-09-14), nunca antes visto renderizado. Solo `apps/web/components/CompraFormV2.tsx` — sin esquema ni RPC (ADR-0098): Serie/Número/Fecha de emisión se superponían entre 1024-1279px; breakpoint del layout de 2 columnas + `sticky` del Resumen movido de `lg:` a `xl:`. No tocó Catálogo/taxonomía (asignado a otra sesión) ni `PLANTILLA_LINEAS`/`ProductoForm.tsx` (mismo patrón, sin disparar — anotado como pendiente). PR #110 mergeado a `main`. | 2026-09-17 |
| Claude (worktree `refresca-diccionario-produccion`) | `claude/refresca-diccionario-produccion` | Refrescó `docs/datos/generado/` contra producción de verdad (solo lectura, MCP de Supabase) — el volcado tenía hora 15:26 y marcaba 3 falsas "pantallas rotas" (`actualizar_categoria_ejes`, `marcar_comprobante_no_emitido`, `actualizar_variantes_etiquetas` de ADR-0093/0095, aplicadas esa tarde-noche). `pnpm datos:comparar` sale limpio ahora. Solo tocó `docs/datos/generado/*` — nada de código ni esquema. PR #112 abierto, esperando merge de Felipe. | 2026-09-17 |
