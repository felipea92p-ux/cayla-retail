abrir_caja(p_ubicacion_id uuid, p_monto_apertura numeric, p_motivo_diferencia text) -> uuid [definer]
abrir_conteo(p_ubicacion_id uuid, p_sububicacion_id uuid, p_alcance text, p_alcance_categoria_id uuid) -> uuid [definer]
abrir_produccion(p_ubicacion_id uuid, p_producto_id uuid, p_lineas jsonb, p_costo_tela numeric, p_costo_avios numeric, p_costo_maquila numeric, p_es_muestra boolean, p_fecha_entrega date, p_nota text, p_token uuid) -> uuid [definer]
actualizar_campana_etiqueta(p_etiqueta_id uuid, p_descuento_pct numeric, p_vigente_desde date, p_vigente_hasta date, p_categoria_ids uuid[]) -> void [definer]
actualizar_categoria(p_categoria_id uuid, p_nombre text, p_familia text, p_prefijo text, p_notas text) -> void [definer]
actualizar_categoria_ejes(p_categoria_id uuid, p_talla_ids uuid[], p_tejido_ids uuid[], p_patron_ids uuid[], p_talla_habitual_ids uuid[]) -> void [definer]
actualizar_mi_foto_perfil(p_foto_url text) -> void [definer]
actualizar_proveedor(p_proveedor_id uuid, p_nombre text, p_ruc text, p_contacto text, p_rubro text, p_plazo_credito_dias integer, p_forma_pago_preferida text, p_telefono text, p_banco text, p_cuenta_bancaria text) -> void [definer]
actualizar_transmision_comprobante(p_comprobante_id uuid, p_estado text, p_entorno text, p_respuesta_sunat jsonb, p_motivo_rechazo text) -> void [definer]
actualizar_variantes_etiquetas(p_asignaciones jsonb) -> void [definer]
agregar_colaborador(p_persona_id uuid, p_ubicacion_id uuid) -> void [definer]
agregar_colaboradores(p_personas uuid[], p_ubicacion_id uuid) -> integer [definer]
agregar_comprador_de_tienda(p_persona_id uuid, p_ubicacion_id uuid) -> void [definer]
agregar_terminal(p_persona_id uuid, p_ubicacion_id uuid, p_terminal text) -> void [definer]
ajustar_insumo_por_conteo(p_insumo_id uuid, p_ubicacion_id uuid, p_cantidad_contada numeric, p_motivo text) -> uuid [definer]
anular_activo(p_activo_id uuid, p_motivo text) -> void [definer]
anular_compra(p_compra_id uuid, p_motivo text) -> void [definer]
anular_comprobante(p_comprobante_id uuid, p_motivo text, p_confirmada boolean, p_respuesta jsonb) -> void [definer]
anular_comprobante_produccion(p_comprobante_id uuid, p_motivo text) -> void [definer]
anular_conciliacion(p_id uuid, p_motivo text) -> void [definer]
anular_conteo(p_conteo_id uuid) -> void [definer]
anular_gasto(p_gasto_id uuid, p_motivo text) -> void [definer]
anular_movimiento_dinero(p_id uuid, p_motivo text) -> void [definer]
anular_produccion(p_produccion_id uuid, p_motivo text) -> void [definer]
anular_venta(p_venta_id uuid, p_motivo text, p_items jsonb) -> void [definer]
apartar_stock(p_variante_id uuid, p_ubicacion_id uuid, p_cantidad integer, p_clienta_nombre text, p_clienta_contacto text, p_vence_el date, p_nota text, p_sububicacion_id uuid, p_token uuid) -> uuid [definer]
aprobar_devolucion(p_devolucion_id uuid, p_reembolso_monto numeric, p_reembolso_metodo text, p_reembolso_cuenta_id uuid) -> TABLE(nota_credito_id uuid, nota_credito_serie text, nota_credito_numero integer) [definer]
archivar_adjunto_compra(p_adjunto_id uuid) -> void [definer]
archivar_caja_prueba(p_caja_id uuid) -> void [definer]
archivar_conteo_prueba(p_conteo_id uuid) -> void [definer]
archivar_cuenta_dinero(p_id uuid, p_archivar boolean) -> void [definer]
archivar_gasto_fijo(p_id uuid) -> void [definer]
archivar_producto_prueba(p_producto_id uuid) -> void [definer]
archivar_rol(p_rol_id uuid) -> void [definer]
archivar_serie_comprobante(p_serie_id uuid, p_motivo text) -> void [definer]
archivar_venta_prueba(p_venta_id uuid) -> void [definer]
asignar_cuenta_pasada(p_clave text, p_cuenta_id uuid) -> uuid [definer]
asignar_rol(p_rol_id uuid, p_persona_id uuid, p_terminal_id uuid, p_ubicacion_id uuid) -> void [definer]
buscar_clienta(p_termino text) -> SETOF retail.clientas [definer]
buscar_productos_parecidos(p_referencia text, p_excluir_id uuid) -> TABLE(id uuid, referencia text, categoria_id uuid, categoria text, nivel text, similitud real) [invoker]
buscar_separaciones(p_ubicacion_id uuid, p_texto text, p_estados text[]) -> TABLE(id uuid, codigo text, estado text, clienta_nombres text, clienta_apellidos text, clienta_celular text, clienta_dni text, asesora text, total numeric, adelanto numeric, saldo numeric, vence_el date, extensiones smallint, creada_en timestamp with time zone, devolucion_medio text, devolucion_numero text, devolucion_cci_final text, liberada_sola boolean, comprobante_anticipo text, comprobante_final text, nota_credito text, items jsonb, pagos jsonb) [definer]
cambiar_estado_proveedor_produccion(p_proveedor_id uuid, p_activo boolean) -> void [definer]
cambiar_tienda_gestora_compra(p_compra_id uuid, p_ubicacion_id uuid) -> void [definer]
cambiar_ubicacion_colaborador(p_persona_id uuid, p_ubicacion_id uuid) -> void [definer]
campanas_vigentes() -> TABLE(variante_id uuid, etiqueta_id uuid, etiqueta_nombre text, descuento_pct numeric) [invoker]
catalogo_actualizar_producto(p_producto_id uuid, p_referencia text, p_estado text, p_variantes jsonb, p_categoria_id uuid, p_descripcion text, p_stock_minimo integer, p_temporada text, p_permitir_venta_sin_stock boolean, p_fotos jsonb, p_tejido_id uuid, p_patron_id uuid, p_marca_id uuid, p_proveedor_id uuid, p_confirmo_distinto boolean, p_version_esperada integer) -> integer [invoker]
catalogo_crear_producto(p_referencia text, p_variantes jsonb, p_categoria_id uuid, p_descripcion text, p_stock_minimo integer, p_temporada text, p_permitir_venta_sin_stock boolean, p_fotos jsonb, p_tejido_id uuid, p_patron_id uuid) -> uuid [invoker]
censo_crear_variante(p_referencia text, p_categoria_id uuid, p_codigo_barras text, p_talla_id uuid, p_color_codigo text, p_costo numeric, p_precio numeric, p_marca_id uuid, p_proveedor_id uuid) -> TABLE(variante_id uuid, sku text, referencia text, talla text, color text, costo numeric, codigo_barras text, reutilizado boolean) [definer]
cerrar_caja(p_caja_id uuid, p_monto_real numeric, p_traslado_monto numeric, p_traslado_destino text, p_traslado_referencia text, p_traslado_cuenta_id uuid) -> TABLE(monto_sistema numeric, monto_real numeric, diferencia numeric, monto_trasladado numeric, monto_fondo numeric) [definer]
cerrar_conteo(p_conteo_id uuid) -> TABLE(lineas_ajustadas integer, unidades_sobrantes integer, unidades_faltantes integer) [definer]
cerrar_linea_compra(p_compra_item_id uuid, p_cantidad integer, p_motivo text, p_nota text, p_ubicacion_id uuid) -> uuid [definer]
cerrar_periodo(p_mes date, p_alcance text, p_ubicacion_id uuid) -> jsonb [definer]
cerrar_produccion(p_produccion_id uuid, p_buenas jsonb, p_costo_tela numeric, p_costo_avios numeric, p_costo_maquila numeric) -> void [definer]
cerrar_traslado_con_diferencia(p_transferencia_id uuid, p_nota text) -> TABLE(lineas_recibidas integer, unidades_recibidas integer) [definer]
compras_nota_pendiente(p_compra_ids uuid[]) -> TABLE(compra_id uuid, unidades_cerradas integer, monto_esperado numeric, resuelto boolean) [definer]
confirmar_traslado(p_transferencia_id uuid) -> TABLE(resultado text, lineas_ok integer, lineas_con_diferencia integer) [definer]
conteo_contar(p_conteo_id uuid, p_variante_id uuid, p_cantidad_contada integer) -> uuid [definer]
convertir_proforma_a_comprobante(p_proforma_id uuid, p_tipo text, p_venta_id uuid, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text) -> uuid [definer]
crear_cuenta_dinero(p_nombre text, p_tipo text, p_saldo_inicial numeric, p_saldo_desde date, p_numero text) -> uuid [definer]
crear_devolucion(p_venta_id uuid, p_ubicacion_id uuid, p_items jsonb, p_motivo text, p_motivo_codigo text) -> uuid [definer]
crear_marca(p_nombre text, p_proveedor_id uuid) -> uuid [definer]
crear_producto_con_variantes(p_referencia text, p_categoria_id uuid, p_variantes jsonb, p_descripcion text, p_token uuid, p_tejido_id uuid, p_patron_id uuid, p_confirmo_distinto boolean, p_etiqueta_ids uuid[], p_marca_id uuid, p_proveedor_id uuid) -> uuid [definer]
crear_proforma(p_ubicacion_id uuid, p_items jsonb, p_cliente_nombre text, p_cliente_num_doc text, p_vence_at timestamp with time zone, p_nota text) -> uuid [definer]
crear_rol(p_nombre text, p_descripcion text, p_copiar_de uuid) -> uuid [definer]
dar_de_baja_activo(p_activo_id uuid, p_fecha date, p_motivo text) -> void [definer]
desactivar_categoria(p_categoria_id uuid) -> void [definer]
desactivar_proveedor(p_proveedor_id uuid) -> void [definer]
desactivar_terminal(p_terminal_id uuid) -> void [definer]
descartar_fijo_sugerido(p_ubicacion_id uuid, p_categoria text, p_proveedor_id uuid) -> void [definer]
deuda_por_vencimiento() -> TABLE(tramo text, comprobantes integer, monto numeric) [definer]
devolver_insumo_de_produccion(p_produccion_id uuid, p_insumo_id uuid, p_cantidad numeric, p_nota text) -> uuid [definer]
editar_cuenta_dinero(p_cuenta_id uuid, p_nombre text, p_numero text, p_tipo text, p_saldo_inicial numeric, p_saldo_desde date) -> void [definer]
eliminar_cuenta_dinero(p_cuenta_id uuid) -> void [definer]
emitir_comprobante(p_ubicacion_id uuid, p_tipo text, p_subtotal numeric, p_igv numeric, p_total numeric, p_venta_id uuid, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text, p_items jsonb, p_token uuid) -> uuid [definer]
emitir_nota(p_comprobante_original_id uuid, p_tipo text, p_motivo text, p_subtotal numeric, p_igv numeric, p_total numeric, p_items jsonb) -> uuid [definer]
entregar_separacion(p_separacion_id uuid, p_pagos jsonb, p_token uuid) -> uuid [definer]
etiquetar_variantes(p_cambios jsonb) -> jsonb [definer]
extender_separacion(p_separacion_id uuid) -> date [definer]
fn_activos_fijos_set_updated_at() -> trigger [invoker]
fn_activos_lista(p_corte date, p_ubicacion_id uuid) -> TABLE(id uuid, ubicacion_id uuid, ubicacion_nombre text, tipo text, tipo_nombre text, cuenta text, nombre text, serie text, fecha_adquisicion date, costo numeric, vida_util_meses integer, depreciacion_mensual numeric, meses_depreciados integer, depreciacion_acumulada numeric, valor_hoy numeric, estado text, fecha_baja date, motivo_baja text, compra_id uuid, comprobante text, comprobante_tipo text, proveedor_nombre text, condicion text, saldo numeric, tiene_pagos boolean, medio_pago text, caja_movimiento_id uuid, motivo_anulacion text, registrado_por_nombre text, creado_en timestamp with time zone) [definer]
fn_activos_sellar() -> trigger [definer]
fn_activos_validar() -> trigger [invoker]
fn_actor_persona_id(p_de_tienda boolean) -> uuid [definer]
fn_admins() -> TABLE(persona_id uuid) [definer]
fn_alcanzo_a(p_persona_id uuid) -> boolean [definer]
fn_anotar_fondo_requerido() -> trigger [definer]
fn_aplicar_candado_de_dinero() -> text[] [invoker]
fn_aplicar_movimiento(p_movimiento_id uuid) -> void [definer]
fn_aprobar_alta_colaborador(p_persona_id uuid) -> void [definer]
fn_asesoras_de_turno(p_ubicacion_id uuid) -> TABLE(persona_id uuid, nombre_corto text, estado_ahora text, es_de_esta_sede boolean) [definer]
fn_asiento_cuenta_de_medio(p_medio text, p_sentido text) -> text [invoker]
fn_asientos(p_desde date, p_hasta date, p_ubicacion_id uuid) -> TABLE(fecha date, ubicacion_id uuid, asiento text, regla text, cuenta text, debe numeric, haber numeric, origen_tabla text, origen_id uuid, glosa text) [definer]
fn_asientos_descuadrados(p_desde date, p_hasta date, p_ubicacion_id uuid) -> TABLE(asiento text, regla text, ubicacion_id uuid, fecha date, debe numeric, haber numeric, diferencia numeric) [definer]
fn_asientos_dinero_y_caja(p_desde date, p_hasta date, p_todo boolean, p_ubics uuid[]) -> TABLE(f date, ubicacion_id uuid, asiento text, regla text, cuenta text, debe numeric, haber numeric, ot text, oid uuid, glosa text) [invoker]
fn_asignar_codigo_producto(p_producto_id uuid) -> text [definer]
fn_asignar_codigo_variante(p_variante_id uuid) -> text [definer]
fn_bal_activos(p_corte date) -> TABLE(activo_id uuid, ubicacion_id uuid, cuenta text, costo numeric, depreciacion numeric, estado text) [invoker]
fn_bal_anticipos(p_corte date) -> TABLE(ubicacion_id uuid, monto numeric) [invoker]
fn_bal_causas_dinero(p_desde date, p_hasta date) -> TABLE(cuenta text, clave text, texto text, monto numeric, acepta boolean) [invoker]
fn_bal_causas_mercaderia(p_desde date, p_hasta date) -> TABLE(clave text, texto text, monto numeric) [invoker]
fn_bal_diario(p_desde date, p_hasta date) -> TABLE(fecha date, ubicacion_id uuid, asiento text, regla text, cuenta text, debe numeric, haber numeric) [invoker]
fn_bal_dinero(p_corte date) -> TABLE(cuenta_dinero_id uuid, tipo text, cuenta_contable text, ubicacion_id uuid, nombre text, saldo numeric, caja_abierta boolean, saldo_inicial numeric, saldo_desde date) [invoker]
fn_bal_facturas(p_corte date) -> TABLE(compra_id uuid, ubicacion_gestion_id uuid, naturaleza text, saldo numeric) [invoker]
fn_bal_facturas_de_tienda(p_corte date) -> TABLE(ubicacion_id uuid, saldo numeric) [invoker]
fn_bal_otro_camino(p_corte date) -> TABLE(cuenta text, saldo numeric) [invoker]
fn_bal_por_recibir(p_corte date) -> numeric [invoker]
fn_bal_saldo_a_favor(p_corte date) -> numeric [invoker]
fn_bal_stock(p_corte date) -> TABLE(ubicacion_id uuid, unidades bigint, valor numeric) [invoker]
fn_bal_transito(p_corte date) -> numeric [invoker]
fn_balance_general(p_corte date, p_ubicacion_id uuid) -> TABLE(seccion text, cuenta text, nombre text, monto numeric, orden integer, detalle jsonb) [definer]
fn_balance_por_tienda(p_corte date) -> TABLE(ubicacion_id uuid, nombre text, tipo text, caja numeric, caja_visible boolean, mercaderia numeric, activos_fijos numeric, activos_costo numeric, activos_depreciacion numeric, facturas_por_pagar numeric, invertido numeric, ventas_mes numeric, utilidad_mes numeric, rinde numeric, desde date, hasta date, planilla_visible boolean) [definer]
fn_bloquear_en_orden(p_ubicacion_id uuid, p_variantes uuid[], p_bloquear_variantes boolean, p_compra_items uuid[]) -> void [invoker]
fn_busqueda_formas_color(p_texto text) -> text[] [invoker]
fn_busqueda_singulares(p_texto text) -> text[] [invoker]
fn_caja_traslados_sellar() -> trigger [definer]
fn_calcular_esperado_caja(p_caja_id uuid) -> TABLE(apertura numeric, ventas_efectivo numeric, ingresos numeric, egresos numeric, reembolsos_efectivo numeric, cambios_efectivo numeric, esperado numeric) [definer]
fn_cambios_sellar() -> trigger [definer]
fn_campanas_por_variante(p_hoy date, p_tolerancia_dias integer, p_variante_ids uuid[]) -> TABLE(variante_id uuid, etiqueta_id uuid, etiqueta_nombre text, descuento_pct numeric) [invoker]
fn_campanas_reporte(p_ubicacion_id uuid) -> TABLE(etiqueta_id uuid, nombre text, desde date, hasta date, descuento_pct numeric, momento text, dias integer, tiendas integer, ventas numeric, normal numeric, descuento numeric, prendas bigint, margen_pct numeric, margen_normal_pct numeric, margen_extra numeric, meta_pct numeric, meta_campana numeric, meta_normal numeric, con_efecto boolean) [definer]
fn_candado_activos_fijos() -> trigger [definer]
fn_candado_compra_notas_credito() -> trigger [definer]
fn_candado_compra_pagos() -> trigger [definer]
fn_candado_compras() -> trigger [definer]
fn_candado_gastos() -> trigger [definer]
fn_candado_movimientos_dinero() -> trigger [definer]
fn_candado_proveedor_creditos() -> trigger [definer]
fn_candado_venta_items() -> trigger [definer]
fn_candado_ventas() -> trigger [definer]
fn_capacidad_por_modulos(p_claves text[]) -> boolean [definer]
fn_catalogo_cambio() -> trigger [definer]
fn_catalogo_version() -> bigint [definer]
fn_categorias_gasto() -> TABLE(codigo text, nombre text, ejemplos text, cuenta text, cuenta_nombre text) [definer]
fn_cerrar_apartado_de_separacion(p_apartado_id uuid, p_motivo text, p_persona uuid) -> uuid [definer]
fn_cierre_mes_estado(p_mes date) -> TABLE(alcance text, ubicacion_id uuid, nombre text, tipo text, orden integer, estado text, cierre jsonb, chequeos jsonb, bloqueantes integer, avisos integer, huella_viva text) [definer]
fn_cierre_panel(p_mes date) -> jsonb [definer]
fn_cierres_con_diferencia(p_ubicacion_id uuid, p_dias integer) -> TABLE(caja_id uuid, ubicacion_nombre text, cerrada_en timestamp with time zone, diferencia numeric, cerrada_por text) [definer]
fn_clave_referencia(p text) -> text [invoker]
fn_clave_texto(p text) -> text [invoker]
fn_colaborador_rol_coherente() -> trigger [invoker]
fn_colaboradores() -> TABLE(persona_id uuid, nombre text, correo text, sede text, rol text, ubicacion_asignada text, agregado_en timestamp with time zone, ubicacion_id uuid, es_yo boolean, ultimo_acceso timestamp with time zone) [definer]
fn_colaboradores_actividad(p_limite integer) -> TABLE(id bigint, accion text, persona_nombre text, por_nombre text, rol text, ubicacion_anterior text, ubicacion_nueva text, motivo text, created_at timestamp with time zone, total bigint) [definer]
fn_colaboradores_inactivos() -> TABLE(persona_id uuid, nombre text, correo text, sede text, estado_dynamic text, rol text, suspendida boolean) [definer]
fn_colaboradores_no_suspendido() -> trigger [invoker]
fn_colaboradores_pendientes() -> TABLE(persona_id uuid, nombre text, correo text, sede text, ubicacion_asignada text, propuesto_por text, propuesto_en timestamp with time zone) [definer]
fn_colaboradores_suspendidos() -> TABLE(persona_id uuid, nombre text, correo text, sede text, rol text, ubicacion_asignada text, suspendido_en timestamp with time zone, suspendido_por_nombre text, motivo text) [definer]
fn_colores_estado_trigger() -> trigger [definer]
fn_compra_cierre_insertado() -> trigger [definer]
fn_compra_es_de_mis_tiendas(p_compra_id uuid) -> boolean [definer]
fn_compra_item_insertado() -> trigger [definer]
fn_compra_item_reparto_cuadra() -> trigger [invoker]
fn_compra_nota_credito_insertada() -> trigger [definer]
fn_compra_pago_insertado() -> trigger [definer]
fn_compra_pagos_sellar() -> trigger [definer]
fn_compra_reasignaciones_inmutable() -> trigger [invoker]
fn_compras_gasto_se_anula_desde_gastos() -> trigger [invoker]
fn_compras_naturaleza_fija() -> trigger [invoker]
fn_compras_ubicaciones() -> uuid[] [definer]
fn_compras_visibles() -> uuid[] [definer]
fn_comprobante_de_venta_no_anulada() -> trigger [definer]
fn_comprobante_y_pago(p_naturaleza text, p_ubicacion_id uuid, p_fecha date, p_monto numeric, p_descripcion text, p_comprobante jsonb, p_medio_pago text, p_caja_id uuid, p_caja_movimiento_id uuid, p_referencia text, p_nota_caja text, p_actor uuid, p_cuenta_id uuid, OUT o_compra_id uuid, OUT o_caja_movimiento_id uuid, OUT o_igv numeric) -> record [definer]
fn_comprobantes_cola_reintento(p_ubicacion_id uuid) -> TABLE(comprobante_id uuid, ubicacion_id uuid, tipo text, serie text, numero integer, venta_id uuid, intentos_transmision integer, ultimo_intento_transmision_at timestamp with time zone, ultimo_error_transmision text, horas_esperando numeric) [definer]
fn_comprobantes_produccion(p_limite integer) -> TABLE(id uuid, proveedor_id uuid, proveedor text, tipo text, serie text, numero text, fecha_emision date, condicion text, fecha_vencimiento date, subtotal numeric, igv numeric, total numeric, estado text, motivo_anulacion text, nota text, created_at timestamp with time zone, lineas bigint, pagado numeric, saldo numeric, estado_pago text, vencido boolean) [definer]
fn_comprobantes_produccion_libro_inmutable() -> trigger [invoker]
fn_conciliacion(p_cuenta_id uuid, p_hasta date) -> jsonb [definer]
fn_conciliacion_contable(p_corte date) -> TABLE(orden integer, clave text, titulo text, contra text, diario numeric, otro numeric, diferencia numeric, estado text, bloquea boolean, causas jsonb, arranque date, corte date) [definer]
fn_conciliacion_cuentas() -> TABLE(id uuid, nombre text, tipo text, saldo_hoy numeric, ultima_fecha date, ultimo_saldo_banco numeric, ultima_diferencia numeric, pendientes integer) [definer]
fn_conciliaciones_solo_anular() -> trigger [invoker]
fn_configuracion_tiendas(p_mes date) -> jsonb [definer]
fn_consumir_saldo_favor(p_proveedor_id uuid, p_monto numeric, p_compra_id uuid, p_compra_pago_id uuid, p_fecha date, p_persona uuid) -> void [definer]
fn_conteos_es_prueba_solo_lider() -> trigger [definer]
fn_conteos_resumen(p_ubicacion_id uuid, p_limite integer) -> TABLE(id uuid, numero integer, estado text, created_at timestamp with time zone, cerrado_en timestamp with time zone, sububicacion_id uuid, sububicacion_nombre text, sububicacion_tipo text, alcance text, alcance_categoria_nombre text, abierto_por uuid, cerrado_por uuid, lineas integer, lineas_con_diferencia integer, sistema integer, contado integer, diferencia integer, soles_diferencia numeric) [invoker]
fn_costo_historial(p_variante_id uuid) -> TABLE(id uuid, created_at timestamp with time zone, stock_previo integer, costo_anterior numeric, cantidad_nueva integer, costo_unitario_nuevo numeric, costo_resultante numeric, origen text, usuario_nombre text, lote_guia text, proveedor_nombre text, compra_documento text, produccion_referencia text) [definer]
fn_costo_historial_inmutable() -> trigger [invoker]
fn_costo_variante_al(p_variante_id uuid, p_instante timestamp with time zone) -> numeric [invoker]
fn_costos_insumos_taller(p_ubicacion_id uuid) -> TABLE(lote_id uuid, costo_unitario numeric) [definer]
fn_costos_producciones(p_ubicacion_id uuid) -> TABLE(produccion_id uuid, costo_tela numeric, costo_avios numeric, costo_maquila numeric, costo_unitario numeric) [definer]
fn_costos_variantes_json(p_ids uuid[]) -> jsonb [definer]
fn_cotizacion_maquila_vigente(p_categoria_id uuid) -> retail.cotizaciones_maquila [definer]
fn_cuenta_de_cajon(p_ubicacion_id uuid) -> uuid [invoker]
fn_cuenta_de_cobro(p_ubicacion_id uuid, p_medio text, p_fecha date) -> uuid [invoker]
fn_cuenta_deudora(p_cuenta text) -> boolean [invoker]
fn_cuenta_dinero_detalle(p_cuenta_id uuid) -> jsonb [definer]
fn_cuenta_sellada(p_clase text, p_medio text, p_ubicacion_id uuid, p_fecha date, p_elegida uuid) -> uuid [definer]
fn_cuenta_sirve(p_clase text, p_medio text, p_tipo text) -> boolean [invoker]
fn_cuentas_asignadas_inmutable() -> trigger [invoker]
fn_cuentas_con_rol() -> TABLE(tipo text, id uuid, nombre text, ubicacion_nombre text, rol_id uuid, es_lider boolean, estado text) [definer]
fn_cuentas_de_sede_nueva() -> trigger [definer]
fn_cuentas_del_rol(p_rol_id uuid) -> integer [definer]
fn_cuentas_dinero_destinos() -> TABLE(id uuid, nombre text) [definer]
fn_cuentas_dinero_inmutable() -> trigger [invoker]
fn_cuentas_dinero_saldos(p_corte date, p_ubicacion_id uuid) -> TABLE(id uuid, nombre text, tipo text, cuenta_contable text, ubicacion_id uuid, ubicacion_nombre text, numero text, saldo numeric, archivada boolean, solo_destino boolean, caja_id uuid, caja_abierta boolean, saldo_desde date, ultima_conciliacion date, ultimo_abono date, orden integer) [definer]
fn_cuentas_dinero_ubicaciones() -> uuid[] [definer]
fn_cuentas_para_elegir(p_clase text, p_ubicacion_id uuid) -> TABLE(id uuid, nombre text, tipo text, ubicacion_id uuid, ubicacion_nombre text, caja_abierta boolean, propuesta_para text[]) [definer]
fn_dentro_de_una_edicion(a text, b text) -> boolean [invoker]
fn_depreciacion_mes(p_mes date) -> TABLE(ubicacion_id uuid, monto numeric) [definer]
fn_descuento_campana(p_precio numeric, p_pct numeric) -> numeric [invoker]
fn_deuda_consolidada() -> TABLE(origen text, proveedor_id uuid, proveedor text, comprobantes bigint, saldo numeric, vencido numeric, proximo_vencimiento date) [definer]
fn_deuda_visible(p_ids uuid[]) -> TABLE(compra_id uuid, total numeric, pagado numeric, saldo numeric, gestionada boolean) [definer]
fn_devoluciones_sellar() -> trigger [definer]
fn_diario(p_desde date, p_hasta date, p_ubicacion_id uuid) -> TABLE(fecha date, ubicacion_id uuid, asiento text, regla text, cuenta text, debe numeric, haber numeric, origen_tabla text, origen_id uuid, glosa text, congelado boolean) [definer]
fn_diario_cerrado_inmutable() -> trigger [invoker]
fn_diario_linea_texto(p_fecha date, p_ubicacion_id uuid, p_asiento text, p_regla text, p_cuenta text, p_debe numeric, p_haber numeric, p_origen_tabla text, p_origen_id uuid, p_glosa text) -> text [invoker]
fn_diario_ubicaciones() -> uuid[] [definer]
fn_dinero_libro(p_hasta date) -> TABLE(cuenta_id uuid, fecha date, monto numeric, clave text, detalle text, ubicacion_id uuid, origen text) [invoker]
fn_dinero_revisados_solo_desmarcar() -> trigger [invoker]
fn_dinero_sin_cuenta(p_desde date, p_hasta date) -> TABLE(origen text, n integer, monto numeric) [definer]
fn_dynamic_disponibles() -> TABLE(persona_id uuid, nombre text, correo text, sede text) [definer]
fn_efectivo_por_tienda(p_ubicacion_id uuid) -> TABLE(ubicacion_id uuid, ubicacion_nombre text, caja_id uuid, abierta boolean, abierta_en timestamp with time zone, abierta_por text, apertura numeric, ventas_efectivo numeric, ingresos numeric, egresos numeric, depositos numeric, reembolsos numeric, cambios numeric, esperado numeric, cerrada_en timestamp with time zone, fondo numeric) [definer]
fn_egreso_ya_usado(p_caja_movimiento_id uuid, p_salvo text) -> text [invoker]
fn_egresos_no_gasto_lista(p_ubicacion_id uuid, p_limite integer) -> TABLE(id uuid, caja_movimiento_id uuid, ubicacion_nombre text, monto numeric, motivo_egreso text, nota_egreso text, tipo text, motivo text, revisado_por_nombre text, revisado_en timestamp with time zone) [definer]
fn_egresos_no_gasto_validar() -> trigger [invoker]
fn_egresos_para_movimiento(p_ubicacion_id uuid) -> TABLE(id uuid, caja_id uuid, monto numeric, motivo text, nota text, creado_en timestamp with time zone, marca text) [definer]
fn_egresos_sin_clasificar(p_ubicacion_id uuid, p_limite integer) -> TABLE(id uuid, caja_id uuid, ubicacion_id uuid, ubicacion_nombre text, monto numeric, motivo text, nota text, registrado_por_nombre text, creado_en timestamp with time zone, caja_abierta boolean) [definer]
fn_es_admin() -> boolean [definer]
fn_es_admin_persona(p_persona_id uuid) -> boolean [definer]
fn_es_lider() -> boolean [definer]
fn_es_lider_persona(p_persona_id uuid) -> boolean [definer]
fn_es_merma(p_tipo text, p_motivo text, p_cantidad integer) -> boolean [invoker]
fn_es_terminal(p_tipo text) -> boolean [definer]
fn_es_traslado_interno(p_tipo text, p_ubicacion_id uuid, p_ubicacion_destino_id uuid) -> boolean [invoker]
fn_es_venta_de_stock(p_tipo text, p_motivo text, p_cambio_id uuid, p_venta_item_id uuid, p_venta_estado text) -> boolean [invoker]
fn_esperado_caja(p_caja_id uuid) -> TABLE(apertura numeric, ventas_efectivo numeric, ingresos numeric, egresos numeric, reembolsos_efectivo numeric, cambios_efectivo numeric, esperado numeric) [definer]
fn_estado_resultados(p_desde date, p_hasta date, p_ubicacion_id uuid) -> TABLE(ubicacion_id uuid, unidad text, nombre text, orden integer, ventas_netas numeric, costo_ventas numeric, fletes numeric, mermas numeric, margen_bruto numeric, planilla numeric, depreciacion numeric, gastos_operacion numeric, resultado numeric, igv_ventas numeric, detalle_gastos jsonb, detalle_mermas jsonb, unidades_sin_costo bigint, mermas_sin_costo bigint, asientos_descuadrados bigint, planilla_visible boolean, planilla_nota text) [definer]
fn_etiquetas_estado_trigger() -> trigger [definer]
fn_exige_dinero_de_compras(p_que text) -> void [invoker]
fn_exige_prenda_regularizada() -> trigger [definer]
fn_exige_responsable() -> boolean [definer]
fn_exigir_alcanzo_a(p_persona_id uuid, p_accion text) -> void [definer]
fn_exigir_egreso_del_pago(p_caja_movimiento_id uuid, p_cuenta_id uuid, p_monto numeric) -> void [definer]
fn_exigir_lider_balance() -> void [definer]
fn_exigir_lider_de_roles() -> void [definer]
fn_exigir_lider_dinero(p_que text) -> void [definer]
fn_exigir_linea_venta_disponible(p_venta_item_id uuid, p_cantidad integer, p_accion text) -> integer [invoker]
fn_exigir_mes_abierto(p_ubicacion_id uuid, p_desde date, p_hasta date, p_para text, p_por text) -> void [definer]
fn_exigir_modulos_dentro_de_lo_mio(p_rol_id uuid, p_modulos text[]) -> void [definer]
fn_exigir_otro_admin(p_persona_id uuid, p_accion text) -> void [definer]
fn_exigir_puede_tocar_colaborador(p_persona_id uuid, p_accion text) -> void [definer]
fn_exigir_rol_de_terminal(p_rol_id uuid, p_modulo text) -> void [definer]
fn_exigir_rol_dentro_de_lo_mio(p_rol_id uuid) -> void [definer]
fn_exigir_tienda_y_campana() -> trigger [definer]
fn_exigir_venta_abierta(p_venta_id uuid, p_para text) -> void [definer]
fn_facturas_para_nota_credito(p_texto text, p_proveedor_id uuid, p_filtro text, p_limite integer) -> TABLE(id uuid, documento text, proveedor_id uuid, proveedor_nombre text, fecha_emision date, fecha_vencimiento date, total numeric, pagado numeric, saldo numeric, estado text, tiene_nota boolean, notas_monto numeric) [definer]
fn_familias_desactivar_candado() -> trigger [invoker]
fn_familias_generar_codigo() -> trigger [invoker]
fn_fecha_de_arranque() -> date [invoker]
fn_flujo_caja_proyeccion(p_semanas integer) -> jsonb [definer]
fn_flujo_caja_real(p_desde date, p_hasta date) -> jsonb [definer]
fn_flujo_disponible(p_corte date) -> numeric [invoker]
fn_flujo_exigir_lider() -> void [definer]
fn_flujo_lado(p_categoria text) -> text [invoker]
fn_flujo_lineas(p_desde date, p_hasta date) -> TABLE(fecha date, categoria text, monto numeric, con_cuenta boolean) [invoker]
fn_flujo_planilla_pagada() -> TABLE(sede_codigo text, ubicacion_id uuid, nombre text, fecha_fin date, pagado numeric) [definer]
fn_flujo_planilla_visible() -> boolean [definer]
fn_fotos_personas(p_ids uuid[]) -> TABLE(persona_id uuid, foto_ruta text) [definer]
fn_fuera_de_mi_alcance() -> TABLE(persona_id uuid) [definer]
fn_gastos_fijos_mes(p_mes date, p_ubicacion_id uuid, p_solo_empresa boolean) -> TABLE(id uuid, ubicacion_id uuid, ubicacion_nombre text, categoria text, categoria_nombre text, descripcion text, proveedor_id uuid, proveedor_nombre text, comprobante_tipo text, monto numeric, monto_variable boolean, dia_del_mes smallint, fecha_esperada date, estado text, gasto_id uuid, gasto_monto numeric) [definer]
fn_gastos_fijos_sugeridos() -> TABLE(ubicacion_id uuid, ubicacion_nombre text, categoria text, categoria_nombre text, proveedor_id uuid, proveedor_nombre text, descripcion text, comprobante_tipo text, monto numeric, dia_del_mes integer, meses integer) [definer]
fn_gastos_fuera_de_lo_normal(p_ubicacion_id uuid, p_solo_empresa boolean, p_hoy date) -> TABLE(llave text, proveedor_id uuid, gasto_fijo_id uuid, proveedor text, ubicacion_id uuid, ubicacion_nombre text, categoria text, categoria_nombre text, mes date, monto numeric, gastos integer, promedio numeric, meses_base integer, exceso_pct numeric, ultimo_gasto_id uuid, ultima_fecha date, ultima_descripcion text) [definer]
fn_gastos_lista(p_desde date, p_hasta date, p_ubicacion_id uuid, p_solo_empresa boolean, p_limite integer) -> TABLE(id uuid, ubicacion_id uuid, ubicacion_nombre text, categoria text, categoria_nombre text, cuenta text, descripcion text, fecha date, monto_total numeric, igv numeric, medio_pago text, caja_movimiento_id uuid, compra_id uuid, comprobante_tipo text, comprobante text, proveedor_nombre text, condicion text, fecha_vencimiento date, saldo numeric, tiene_pagos boolean, estado text, motivo_anulacion text, registrado_por_nombre text, creado_en timestamp with time zone) [definer]
fn_gastos_panel(p_desde date, p_hasta date, p_ubicacion_id uuid, p_solo_empresa boolean) -> jsonb [definer]
fn_gastos_puede(p_ubicacion_id uuid) -> boolean [definer]
fn_gastos_sellar() -> trigger [definer]
fn_gastos_solo_anular() -> trigger [invoker]
fn_gastos_ubicaciones() -> uuid[] [definer]
fn_gastos_validar() -> trigger [invoker]
fn_gestora_con_parte() -> trigger [definer]
fn_historial_colaborador(p_persona_id uuid, p_accion text, p_rol text, p_ubicacion_anterior uuid, p_ubicacion_nueva uuid, p_motivo text) -> void [definer]
fn_historial_colaboradores_inmutable() -> trigger [invoker]
fn_historial_es_inmutable() -> trigger [invoker]
fn_historial_producto_cambios(p_producto_id uuid) -> TABLE(id uuid, created_at timestamp with time zone, entidad text, campo text, valor_anterior text, valor_nuevo text, categoria_anterior_nombre text, categoria_nueva_nombre text, variante_id uuid, variante_sku text, variante_talla text, variante_color text, usuario_id uuid, usuario_nombre text) [definer]
fn_historial_producto_cambios_inmutable() -> trigger [invoker]
fn_historial_sin_truncate() -> trigger [invoker]
fn_hoy_lima() -> date [invoker]
fn_huella_congelada(p_cierre_id uuid) -> text [definer]
fn_huella_de_textos(p_textos text[]) -> text [invoker]
fn_ids_de_items(p_items jsonb, p_clave text) -> uuid[] [invoker]
fn_igv_credito_fiscal(p_mes date) -> TABLE(mes date, igv_compras numeric, igv_produccion numeric, igv_notas_credito numeric, igv_neto numeric) [definer]
fn_impuestos_compras(p_desde date, p_hasta date) -> TABLE(origen text, id uuid, naturaleza text, fecha date, tipo text, base numeric, igv numeric, total numeric, da_credito boolean) [definer]
fn_impuestos_igv_meses(p_desde date, p_hasta date) -> TABLE(mes date, comprobantes integer, base_ventas numeric, igv_ventas numeric, igv_notas_credito numeric, debito numeric, credito_mercaderia numeric, credito_gastos numeric, credito_activos numeric, credito_taller numeric, credito_notas numeric, credito numeric, saldo_anterior numeric, a_pagar numeric, saldo_a_favor numeric) [definer]
fn_impuestos_panel(p_mes date) -> jsonb [definer]
fn_impuestos_registro_compras(p_mes date) -> TABLE(origen text, naturaleza text, fecha date, vencimiento date, tipo text, serie text, numero text, proveedor_ruc text, proveedor text, base numeric, igv numeric, no_gravado numeric, total numeric, da_credito boolean, ref_fecha date, ref_tipo text, ref_serie text, ref_numero text, tienda text) [definer]
fn_impuestos_registro_ventas(p_mes date) -> TABLE(fecha date, tipo text, serie text, numero integer, cliente_tipo_doc text, cliente_num_doc text, cliente_nombre text, base numeric, igv numeric, total numeric, estado text, anulado boolean, ref_fecha date, ref_tipo text, ref_serie text, ref_numero integer, tienda text, observacion text) [definer]
fn_impuestos_ventas(p_desde date, p_hasta date) -> TABLE(comprobante_id uuid, fecha date, tipo text, estado text, cuenta boolean, signo integer, base numeric, igv numeric, total numeric) [definer]
fn_insertar_nota_credito_compra(p_compra_id uuid, p_serie_numero text, p_fecha date, p_monto numeric, p_motivo text, p_nota text, p_cierre_id uuid, p_persona uuid) -> uuid [definer]
fn_insertar_reembolso_proveedor(p_proveedor_id uuid, p_monto numeric, p_metodo text, p_referencia text, p_fecha date, p_nota text, p_persona uuid, p_nota_credito_id uuid, p_cuenta_id uuid) -> uuid [definer]
fn_insumos_creado_por() -> trigger [definer]
fn_insumos_set_updated_at() -> trigger [invoker]
fn_ledger_puntos(p_ubicacion_id uuid, p_desde timestamp with time zone, p_variante_ids uuid[]) -> TABLE(variante_id uuid, bucket text, ts timestamp with time zone, ord smallint, oid uuid, delta integer, nivel integer, es_venta boolean, es_interno boolean) [invoker]
fn_ledger_timeline(p_variante_id uuid, p_ubicacion_id uuid, p_desde timestamp with time zone, p_hasta timestamp with time zone) -> TABLE(bucket text, inicio timestamp with time zone, fin timestamp with time zone, nivel integer) [definer]
fn_libro_compras_es_inmutable() -> trigger [invoker]
fn_libro_creditos_es_inmutable() -> trigger [invoker]
fn_libro_recepcion_produccion_inmutable() -> trigger [invoker]
fn_linea_de_venta_no_anulada() -> trigger [definer]
fn_lineas_comprobantes_produccion(p_ubicacion_id uuid, p_comprobante_id uuid) -> TABLE(comprobante_id uuid, proveedor text, tipo text, serie text, numero text, fecha_emision date, item_id uuid, insumo_id uuid, insumo text, unidad text, facturado numeric, recibido numeric, cerrado numeric, pendiente numeric) [definer]
fn_marcar_reintento_transmision(p_comprobante_id uuid, p_error text) -> void [definer]
fn_marcas_desactivar_candado() -> trigger [invoker]
fn_medios_de_cobro() -> TABLE(ubicacion_id uuid, ubicacion_nombre text, medio text, cuenta_id uuid, cuenta_nombre text, vigente_desde date) [definer]
fn_medios_de_cobro_validar() -> trigger [invoker]
fn_mes_cerrado(p_ubicacion_id uuid, p_fecha date) -> boolean [definer]
fn_mes_nombre(p_mes date) -> text [invoker]
fn_meses_depreciados(p_fecha_adquisicion date, p_vida integer, p_corte date, p_fecha_baja date) -> integer [invoker]
fn_meta_mes(p_ubicacion_id uuid, p_mes date) -> numeric [definer]
fn_mi_parte_de_compra(p_compra_id uuid) -> jsonb [definer]
fn_mi_perfil() -> TABLE(persona_id uuid, nombres text, apellidos text, correo text, celular text, foto_url text, rol text, estado text, ubicacion_nombre text, ultimo_acceso timestamp with time zone) [definer]
fn_mi_rol_id() -> uuid [definer]
fn_mi_terminal() -> text [definer]
fn_mis_modulos() -> TABLE(clave text, completo boolean) [definer]
fn_mis_partes_de_compras() -> TABLE(compra_id uuid, documento text, tipo text, proveedor_id uuid, proveedor_nombre text, fecha_emision date, fecha_vencimiento date, estado text, gestora_id uuid, gestora_nombre text, ubicacion_id uuid, ubicacion_nombre text, unidades integer, total numeric, pagado numeric, saldo numeric, registrada_en timestamp with time zone, parte_nueva boolean) [definer]
fn_modulos_de_persona(p_persona_id uuid) -> text[] [definer]
fn_modulos_que_no_tengo(p_modulos text[]) -> text[] [definer]
fn_movimiento_compra_insertado() -> trigger [definer]
fn_movimiento_de_cajon(p_cuenta_id uuid, p_tipo text, p_monto numeric, p_fecha date, p_motivo text, p_nota text) -> uuid [definer]
fn_movimiento_no_sale_de_cuarentena_a_la_clienta() -> trigger [invoker]
fn_movimientos(p_ubicacion_id uuid, p_desde date, p_hasta date, p_categoria text, p_motivo text, p_busqueda text, p_usuario_id uuid, p_sububicacion_id uuid, p_cursor_creado_en timestamp with time zone, p_cursor_id uuid, p_limite integer, p_producto_id uuid) -> TABLE(id uuid, created_at timestamp with time zone, fecha_lima date, hora text, tipo text, categoria text, motivo text, cantidad integer, delta integer, es_sistema boolean, nota text, variante_id uuid, sku text, referencia text, talla text, color text, ubicacion_id uuid, ubicacion_nombre text, ubicacion_destino_id uuid, ubicacion_destino_nombre text, sububicacion_id uuid, sububicacion_nombre text, sububicacion_tipo text, sububicacion_destino_id uuid, sububicacion_destino_nombre text, sububicacion_destino_tipo text, usuario_id uuid, usuario_nombre text, venta_id uuid, venta_nota text, comprobante_tipo text, comprobante_numero text, comprobante_estado text, lote_id uuid, lote_guia text, lote_nota text, proveedor_nombre text, compra_id uuid, compra_documento text, transferencia_id uuid, transferencia_estado text, transferencia_nota text, conteo_id uuid, conteo_cantidad_sistema integer, conteo_cantidad_contada integer, devolucion_id uuid, devolucion_motivo text, devolucion_estado text, cambio_id uuid, cambio_diferencia numeric, transferencia_numero integer, conteo_numero integer) [definer]
fn_movimientos_busqueda(p_busqueda text) -> TABLE(variante_ids uuid[], movimiento_ids uuid[]) [invoker]
fn_movimientos_de_comprobante(p_tipos text[], p_serie text, p_numero integer) -> uuid[] [invoker]
fn_movimientos_dinero(p_ubicacion_id uuid, p_limite integer) -> TABLE(id uuid, tipo text, fecha date, cuenta_origen_id uuid, origen_nombre text, origen_tipo text, cuenta_destino_id uuid, destino_nombre text, destino_tipo text, monto numeric, comision numeric, referencia text, ubicacion_id uuid, estado text, motivo_anulacion text, registrado_por_nombre text, anulado_por_nombre text, caja_movimiento_id uuid, creado_en timestamp with time zone, puede_anular boolean) [definer]
fn_movimientos_dinero_validar() -> trigger [invoker]
fn_movimientos_resumen(p_ubicacion_id uuid, p_desde date, p_hasta date, p_motivo text, p_busqueda text, p_usuario_id uuid, p_sububicacion_id uuid) -> TABLE(categoria text, movimientos bigint, unidades bigint, delta bigint) [definer]
fn_movimientos_variantes(p_busqueda text) -> uuid[] [invoker]
fn_nombres_personas(p_ids uuid[]) -> TABLE(id uuid, nombre text) [definer]
fn_pago_no_supera_tienda() -> trigger [definer]
fn_pagos_sin_cuenta() -> TABLE(clave text, origen text, fecha date, detalle text, medio text, monto numeric, ubicacion_nombre text) [definer]
fn_parametro_tributario(p_nombre text, p_fecha date) -> TABLE(valor numeric, texto text, vigente_desde date, provisional boolean, nota text) [definer]
fn_parametros_caja(p_ubicacion_id uuid, p_fecha date) -> TABLE(meta numeric, meta_base numeric, meta_pct numeric, fondo numeric, fondo_base numeric, campanas jsonb) [definer]
fn_parametros_finanzas() -> jsonb [definer]
fn_parametros_tributarios_inmutable() -> trigger [invoker]
fn_parametros_tributarios_lista() -> TABLE(nombre text, vigente_desde date, valor numeric, texto text, nota text, provisional boolean, registrado_por text, registrado_en timestamp with time zone, correcciones integer) [definer]
fn_patrones_estado_trigger() -> trigger [definer]
fn_periodo_cierres_inmutable() -> trigger [invoker]
fn_periodos_mes(p_mes date) -> TABLE(alcance text, ubicacion_id uuid, estado text, huella text, cerrado_en timestamp with time zone) [definer]
fn_periodos_no_se_borran() -> trigger [invoker]
fn_persona_actual_resumen() -> TABLE(nombre text, es_lider boolean, ubicacion_id uuid, ubicacion_nombre text, ubicacion_tipo text) [definer]
fn_persona_presente(p_persona_id uuid, p_ubicacion_id uuid, p_momento timestamp with time zone) -> boolean [definer]
fn_planilla_periodos(p_desde date, p_hasta date) -> TABLE(ubicacion_id uuid, sede_codigo text, periodo_id uuid, fecha_ini date, fecha_fin date, personas bigint, costo numeric) [definer]
fn_planilla_visible() -> boolean [definer]
fn_plata_del_dueno() -> TABLE(id uuid, tipo text, fecha date, monto numeric, referencia text, cuenta_nombre text) [definer]
fn_por_pagar_consolidado(p_ubicacion_id uuid, p_hasta date, p_solo_empresa boolean) -> TABLE(origen text, id uuid, naturaleza text, proveedor_id uuid, proveedor text, documento text, tipo text, concepto text, fecha_emision date, condicion text, fecha_vencimiento date, vence date, ubicacion_id uuid, unidades text, parte boolean, total numeric, pagado numeric, saldo numeric) [definer]
fn_prendas_por_regularizar_al_anular() -> trigger [definer]
fn_presupuesto_configuracion(p_mes date) -> jsonb [definer]
fn_presupuesto_lineas(p_mes date) -> TABLE(cuenta text, nombre text, ejemplos text, orden integer) [definer]
fn_presupuesto_propuesta(p_mes date, p_origen text, p_hoy date) -> TABLE(ubicacion_id uuid, cuenta text, actual numeric, propuesto numeric, desde date, hasta date) [definer]
fn_presupuesto_vs_real(p_mes date, p_ubicacion_id uuid, p_hoy date) -> TABLE(ubicacion_id uuid, unidad text, nombre text, orden integer, linea text, linea_nombre text, tipo text, orden_linea integer, presupuesto numeric, a_la_fecha numeric, proyeccion numeric, fijo numeric, avance numeric, se_pasa boolean, estado text, momento text, dia integer, dias integer) [definer]
fn_presupuestos_no_se_borran() -> trigger [invoker]
fn_presupuestos_validar() -> trigger [definer]
fn_primer_mes_cerrado(p_ubicacion_id uuid, p_desde date, p_hasta date) -> date [definer]
fn_prioridad_conteo(p_ubicacion_id uuid, p_alcance_categoria_id uuid) -> TABLE(variante_id uuid, sku text, referencia text, talla text, color text, sububicacion_id uuid, dias_sin_contar integer, valor_en_riesgo numeric) [definer]
fn_produccion_pagos_sellar() -> trigger [definer]
fn_productos(p_busqueda text, p_categoria_id uuid, p_color_codigo text, p_estado text, p_precio_min numeric, p_precio_max numeric, p_stock text, p_pagina integer, p_por_pagina integer, p_orden text, p_marca_id uuid, p_proveedor_id uuid) -> TABLE(total_productos bigint, producto_id uuid, referencia text, codigo text, categoria_id uuid, categoria_nombre text, estado text, stock_minimo integer, stock_total integer, demanda_diaria numeric, lead_time_dias numeric, punto_reorden integer, reponer_de_proveedor boolean, variante_id uuid, variante_codigo text, sku text, talla text, color_codigo text, color_nombre text, color_hex text, foto_url text, precio numeric, costo numeric, activo boolean, codigos_barras text[], marca_id uuid, marca_nombre text, proveedor_id uuid, proveedor_nombre text) [definer]
fn_productos_buscar(p_busqueda text) -> uuid[] [invoker]
fn_productos_estado_alta_trigger() -> trigger [definer]
fn_productos_por_categoria() -> TABLE(categoria_id uuid, n bigint) [invoker]
fn_productos_referencia_trigger() -> trigger [invoker]
fn_productos_resumen(p_busqueda text, p_categoria_id uuid, p_color_codigo text, p_estado text, p_precio_min numeric, p_precio_max numeric, p_marca_id uuid, p_proveedor_id uuid) -> TABLE(total_productos bigint, total_variantes bigint, stock_bajo bigint, sin_stock bigint, reponer_de_proveedor bigint) [definer]
fn_proveedor_costo_evolucion(p_proveedor_id uuid, p_limite integer) -> TABLE(producto_id uuid, referencia text, compra_id uuid, documento text, fecha date, costo_unitario numeric) [definer]
fn_proveedor_creditos(p_proveedor_id uuid, p_limite integer) -> TABLE(id uuid, tipo text, monto numeric, fecha date, documento text, nota_serie_numero text, metodo text, referencia text, nota text, registrado_por uuid, created_at timestamp with time zone) [definer]
fn_proveedor_creditos_sellar() -> trigger [definer]
fn_proveedor_devoluciones(p_proveedor_id uuid) -> TABLE(unidades bigint, ultima date) [definer]
fn_proveedor_metricas_compras(p_proveedor_id uuid) -> TABLE(facturas_vigentes bigint, total_facturado numeric, saldo numeric, ultima_compra date, facturas_vencidas bigint, facturas_recibidas_completas bigint, facturas_con_recepcion_pendiente bigint, facturas_atrasadas bigint, facturado_12m numeric, monto_vencido numeric, entregado_completo_pct numeric, dias_entrega_promedio numeric, dias_entrega_muestra integer, dias_pago_real_promedio numeric, dias_pago_muestra integer) [definer]
fn_proveedor_metricas_insumos(p_proveedor_id uuid) -> TABLE(lotes bigint, total_comprado numeric, ultima_entrega date) [definer]
fn_proveedor_produccion_metricas(p_proveedor_id uuid) -> TABLE(lotes bigint, total_comprado numeric, ultima_entrega date) [definer]
fn_proveedores() -> TABLE(id uuid, nombre text, ruc text, contacto text, telefono text, banco text, cuenta_bancaria text, activo boolean, facturas bigint, total_facturado numeric, saldo numeric, ultima_compra date, facturas_vencidas bigint, facturas_recibidas_completas bigint, facturas_con_recepcion_pendiente bigint, facturas_atrasadas bigint, rubro text, plazo_credito_dias integer, forma_pago_preferida text, facturado_12m numeric, saldo_vencido numeric, dias_desde_ultima_compra integer, entregas_por_recibir bigint, saldo_favor numeric, cci text, celular_billetera text, billeteras text[], titular_cuenta text) [definer]
fn_proveedores_produccion() -> TABLE(id uuid, nombre text, rubro text, ruc text, contacto text, telefono text, plazo_credito_dias integer, forma_pago_preferida text, banco text, cuenta_bancaria text, cci text, celular_billetera text, billeteras text[], titular_cuenta text, activo boolean, lotes bigint, total_comprado numeric, ultima_entrega date) [definer]
fn_proveedores_resumen() -> TABLE(activos integer, desactivados integer, deuda_total numeric, con_saldo integer, con_vencidas integer, top_proveedor_id uuid, top_proveedor_nombre text, top_pct numeric, top3_pct numeric, sin_compras_90d integer, saldo_favor_total numeric, con_saldo_favor integer) [definer]
fn_proveedores_serie_12m() -> TABLE(proveedor_id uuid, mes date, monto numeric) [definer]
fn_puede_administrar_roles() -> boolean [invoker]
fn_puede_ajustar_inventario() -> boolean [invoker]
fn_puede_analizar() -> boolean [invoker]
fn_puede_comprar_en(p_ubicacion_id uuid) -> boolean [definer]
fn_puede_dar_descuento_por_etiqueta() -> boolean [invoker]
fn_puede_editar_catalogo() -> boolean [invoker]
fn_puede_editar_cuentas_proveedor() -> boolean [invoker]
fn_puede_editar_etiquetas() -> boolean [invoker]
fn_puede_gestionar_caja() -> boolean [invoker]
fn_puede_gestionar_colaboradores() -> boolean [invoker]
fn_puede_gestionar_proveedores() -> boolean [invoker]
fn_puede_operar_ubicacion(p_ubicacion_id uuid) -> boolean [invoker]
fn_puede_pagar_compras() -> boolean [invoker]
fn_puede_registrar_compras() -> boolean [invoker]
fn_puede_registrar_facturas_compra() -> boolean [invoker]
fn_puede_registrar_notas_credito() -> boolean [invoker]
fn_puede_tocar_etiqueta(p_etiqueta_id uuid, p_descuento_nuevo numeric) -> boolean [definer]
fn_puede_ver_compra(p_compra_id uuid) -> boolean [definer]
fn_puede_ver_dinero_de_compras() -> boolean [invoker]
fn_recalcular_costo_insumos_produccion(p_produccion_id uuid) -> void [definer]
fn_recalcular_costo_variante(p_variante_id uuid, p_cantidad_nueva integer, p_costo_unitario_nuevo numeric, p_origen text, p_movimiento_id uuid) -> numeric [definer]
fn_registrar_cambio_producto() -> trigger [definer]
fn_reservar_numero_serie(p_ubicacion_id uuid, p_tipo text) -> TABLE(serie text, numero integer) [definer]
fn_resumen_caja(p_caja_id uuid) -> jsonb [definer]
fn_resumen_comparacion(p_ubicacion_id uuid, p_a_desde date, p_a_hasta date, p_b_desde date, p_b_hasta date) -> TABLE(variante_id uuid, producto_id uuid, referencia text, categoria_id uuid, categoria_nombre text, producto_estado text, producto_codigo text, sku text, codigo text, codigos_barras text[], talla text, color_codigo text, color_nombre text, color_hex text, costo numeric, estado_costo text, ledger_consistente boolean, a_ventas integer, a_devoluciones integer, a_importe numeric, a_costo_ventas numeric, a_costo_devoluciones numeric, a_uds_sin_costo integer, a_entradas integer, a_stock_inicio integer, a_stock_cierre integer, a_dias_con_stock numeric, b_ventas integer, b_devoluciones integer, b_importe numeric, b_costo_ventas numeric, b_costo_devoluciones numeric, b_uds_sin_costo integer, b_entradas integer, b_stock_inicio integer, b_stock_cierre integer, b_dias_con_stock numeric, a_piso_promedio numeric, b_piso_promedio numeric, a_total_promedio numeric, b_total_promedio numeric, ultima_venta_en timestamp with time zone, piso_expuesto_desde_ultima_venta_dias numeric, piso_eventos jsonb, stock_piso_hoy integer, stock_almacen_hoy integer) [definer]
fn_resumen_comparacion_json(p_ubicacion_id uuid, p_a_desde date, p_a_hasta date, p_b_desde date, p_b_hasta date) -> jsonb [invoker]
fn_resumen_finanzas(p_ubicacion_id uuid, p_solo_empresa boolean) -> jsonb [definer]
fn_resumen_variantes(p_ubicacion_id uuid, p_ventana_dias integer, p_desde date, p_hasta date, p_cmp_desde date, p_cmp_hasta date) -> TABLE(variante_id uuid, producto_id uuid, referencia text, categoria_nombre text, sku text, codigo text, talla text, color_codigo text, color_nombre text, color_hex text, foto_url text, stock_minimo integer, separa_piso_almacen boolean, piso integer, almacen integer, sin_sububicacion integer, cuarentena integer, disponible integer, primer_ingreso timestamp with time zone, dias_observables integer, ventas_ventana integer, devoluciones_ventana integer, ultima_venta timestamp with time zone, entradas_ventana integer, mermas_ventana integer, en_camino integer, en_camino_a_tiempo integer, en_camino_atrasado boolean, proxima_llegada timestamp with time zone, traslados_salida_ventana integer, en_red jsonb, categoria_id uuid, producto_estado text, producto_codigo text, precio numeric, costo numeric, estado_costo text, codigos_barras text[], dias_con_stock numeric, stock_inicial integer, ledger_consistente boolean, ventas_cmp integer, devoluciones_cmp integer, dias_con_stock_cmp numeric, origen_abastecimiento text, proximo_traslado_id uuid) [definer]
fn_resumen_variantes_json(p_ubicacion_id uuid, p_desde date, p_hasta date, p_cmp_desde date, p_cmp_hasta date) -> jsonb [invoker]
fn_rls_reescribir(p_expr text) -> text [invoker]
fn_rls_una_vez_por_consulta() -> integer [invoker]
fn_rol_dentro_de_lo_mio(p_rol_id uuid) -> boolean [definer]
fn_rol_modulos_coherente() -> trigger [invoker]
fn_rol_por_clave(p_clave text) -> uuid [definer]
fn_roles_historial_inmutable() -> trigger [invoker]
fn_saldo_de_tienda(p_compra_id uuid, p_ubicacion_id uuid) -> numeric [definer]
fn_saldo_favor_proveedor(p_proveedor_id uuid) -> numeric [invoker]
fn_saldos_iniciales() -> TABLE(id uuid, fecha date, cuenta text, nombre text, tipo text, monto numeric, debe numeric, haber numeric, origen text, nota text, motivo text, reemplaza_id uuid, vigente boolean, registrado_por_nombre text, creado_en timestamp with time zone) [definer]
fn_saldos_iniciales_inmutable() -> trigger [invoker]
fn_saldos_iniciales_propuesta(p_fecha date) -> TABLE(cuenta text, nombre text, tipo text, monto numeric, origen text, detalle text, vigente numeric, orden integer) [definer]
fn_saldos_iniciales_vigentes() -> TABLE(id uuid, fecha date, cuenta text, debe numeric, haber numeric, origen text, nota text) [invoker]
fn_sellar_terminal() -> trigger [definer]
fn_sello_caja(p_caja_id uuid) -> text [definer]
fn_separacion_pagos_sellar() -> trigger [definer]
fn_separaciones_sellar() -> trigger [definer]
fn_siguiente_correlativo(p_prefijo text) -> integer [definer]
fn_soles_diferencia_conteo(p_conteo_id uuid) -> numeric [definer]
fn_stock_por_sede() -> TABLE(variante_id uuid, ubicacion_id uuid, cantidad integer) [definer]
fn_stock_por_sede_json() -> jsonb [invoker]
fn_subir_version() -> trigger [invoker]
fn_subir_version_rol_por_modulos() -> trigger [definer]
fn_sububicacion_por_defecto(p_ubicacion_id uuid, p_uso text) -> uuid [invoker]
fn_suspendidos_no_colaborador() -> trigger [invoker]
fn_tallas_estado_trigger() -> trigger [definer]
fn_tasa_igv(p_fecha date) -> numeric [definer]
fn_tejidos_estado_trigger() -> trigger [definer]
fn_terminal_actual() -> TABLE(id uuid, ubicacion_id uuid, tipo text, nombre text) [definer]
fn_terminal_rol_coherente() -> trigger [invoker]
fn_terminales() -> TABLE(id uuid, nombre text, ubicacion_id uuid, ubicacion_nombre text, rol_id uuid, rol_nombre text, correo text, activo boolean, creada_at timestamp with time zone, desactivada_at timestamp with time zone, ultimo_acceso timestamp with time zone) [definer]
fn_texto_movimiento_dinero(p_tipo text) -> text [invoker]
fn_texto_o_null(p text) -> text [invoker]
fn_texto_periodo(p_mes date, p_alcance text, p_ubicacion_id uuid) -> text [definer]
fn_tiene_acceso_retail() -> boolean [definer]
fn_tipos_activo() -> TABLE(codigo text, nombre text, ejemplos text, cuenta text, cuenta_nombre text, vida_util_meses integer) [definer]
fn_titulo_referencia(p text) -> text [invoker]
fn_token_talla(p_talla text) -> text [invoker]
fn_tomar_comprobantes_para_reintento(p_ubicacion_id uuid, p_limite integer) -> SETOF uuid [definer]
fn_totales_historial_ventas(p_desde timestamp with time zone, p_hasta timestamp with time zone, p_sede_id uuid, p_vendedor_id uuid, p_estado text, p_pago text, p_comprobante text, p_incluir_prueba boolean, p_ids uuid[]) -> jsonb [definer]
fn_traslado_lineas(p_transferencia_id uuid) -> TABLE(variante_id uuid, sku text, referencia text, talla text, color text, cantidad_enviada integer, cantidad_recibida integer, diferencia integer) [definer]
fn_ubicacion_actual_persona() -> uuid [definer]
fn_ubicacion_de_partida(p_persona_id uuid) -> uuid [definer]
fn_unidad_nombre(p_alcance text, p_ubicacion_id uuid) -> text [definer]
fn_usos_cuenta_dinero(p_cuenta_id uuid) -> TABLE(tabla text, n bigint) [definer]
fn_valida_categoria_subcategoria() -> trigger [invoker]
fn_valida_nota_referencia_aceptada() -> trigger [invoker]
fn_validar_fecha_pago_compra(p_fecha date, p_fecha_emision date, p_documento text) -> void [invoker]
fn_validar_marca_proveedor(p_marca_id uuid, p_proveedor_id uuid) -> void [invoker]
fn_variante_permitida_en_sede(p_variante_id uuid, p_ubicacion_id uuid) -> boolean [invoker]
fn_variantes_asignar_codigo() -> trigger [invoker]
fn_ve_modulo(p_clave text) -> boolean [definer]
fn_vencer_separaciones(p_ubicacion_id uuid) -> integer [definer]
fn_venta_pagos_sellar() -> trigger [definer]
fn_ventas_del_dia(p_ubicacion_id uuid) -> TABLE(venta_id uuid, hora text, ubicacion_nombre text, vendedor text, cliente_nombre text, items jsonb, total numeric, metodos_pago text, comprobante_tipo text, comprobante_texto text, comprobante_estado text, nota text) [definer]
fn_verificar_apartados() -> TABLE(variante_id uuid, ubicacion_id uuid, sububicacion_id uuid, en_stock integer, en_apartados bigint) [definer]
fn_verificar_separaciones() -> TABLE(separacion_id uuid, codigo text, problema text) [definer]
guardar_cuentas_proveedor(p_proveedor_id uuid, p_cci text, p_celular_billetera text, p_billeteras text[], p_titular_cuenta text) -> void [definer]
guardar_efecto_campana(p_etiqueta_id uuid, p_ubicacion_id uuid, p_meta_pct numeric, p_fondo numeric) -> void [definer]
guardar_gasto_fijo(p_id uuid, p_ubicacion_id uuid, p_categoria text, p_descripcion text, p_proveedor_id uuid, p_comprobante_tipo text, p_monto numeric, p_monto_variable boolean, p_dia_del_mes integer) -> uuid [definer]
guardar_hora_cierre_tienda(p_ubicacion_id uuid, p_hora time without time zone) -> void [definer]
guardar_medio_de_cobro(p_ubicacion_id uuid, p_medio text, p_cuenta_id uuid) -> void [definer]
guardar_metas_tienda(p_ubicacion_id uuid, p_metas numeric[], p_fondo numeric) -> void [definer]
guardar_modulos_rol(p_rol_id uuid, p_modulos text[], p_version_esperada integer) -> integer [definer]
guardar_parametro_tributario(p_nombre text, p_vigente_desde date, p_valor numeric, p_texto text) -> uuid [definer]
guardar_parametros_finanzas(p_minimo_caja numeric, p_aviso_gasto_pct numeric, p_aviso_vence_dias integer) -> void [definer]
guardar_presupuesto(p_mes date, p_ubicacion_id uuid, p_cuenta text, p_monto numeric) -> void [definer]
guardar_presupuesto_lote(p_mes date, p_filas jsonb, p_origen text) -> integer [definer]
guardar_proveedor_produccion(p_proveedor_id uuid, p_nombre text, p_rubro text, p_ruc text, p_contacto text, p_telefono text, p_plazo_credito_dias integer, p_forma_pago_preferida text, p_banco text, p_cuenta_bancaria text, p_cci text, p_celular_billetera text, p_billeteras text[], p_titular_cuenta text) -> uuid [definer]
iniciar_traslado(p_ubicacion_origen_id uuid, p_ubicacion_destino_id uuid, p_items jsonb, p_fecha_estimada_llegada timestamp with time zone, p_nota text, p_token uuid) -> uuid [definer]
liberar_apartado(p_apartado_id uuid, p_motivo text) -> void [definer]
liberar_separacion(p_separacion_id uuid, p_motivo text) -> void [definer]
lineas_compra_operativo(p_compra_ids uuid[], p_ubicacion_id uuid) -> TABLE(id uuid, compra_id uuid, producto_id uuid, variante_id uuid, descripcion text, cantidad integer, cantidad_facturada integer, recibido bigint, cerrado bigint, pendiente bigint, asignado_aqui integer, recibido_aqui bigint, cerrado_aqui bigint, pendiente_aqui bigint, otras_tiendas jsonb) [definer]
liquidar_prenda_danada(p_id uuid, p_precio_unitario numeric, p_metodo_pago text, p_nota text) -> uuid [definer]
listar_apartados(p_ubicacion_id uuid) -> TABLE(id uuid, variante_id uuid, sku text, referencia text, talla text, color text, sububicacion_id uuid, cantidad integer, clienta_nombre text, clienta_contacto text, nota text, vence_el date, created_at timestamp with time zone, creado_por uuid, creado_por_nombre text, puede_liberar boolean) [definer]
listar_compras(p_limite integer, p_cursor_fecha date, p_cursor_creado_en timestamp with time zone, p_cursor_id uuid, p_orden text, p_busqueda text, p_proveedor_id uuid, p_estado_pago text, p_estado_recepcion text, p_condicion text, p_solo_vigentes boolean, p_con_saldo boolean, p_solo_vencidas boolean, p_por_recibir boolean, p_desde date, p_hasta date, p_tipo text, p_ubicacion_id uuid, p_naturaleza text) -> SETOF retail.compras_resumen [invoker]
listar_compras_operativo(p_limite integer, p_cursor_fecha date, p_cursor_creado_en timestamp with time zone, p_cursor_id uuid, p_busqueda text, p_proveedor_id uuid, p_estado_recepcion text, p_por_recibir boolean, p_desde date, p_hasta date, p_tipo text, p_ubicacion_id uuid) -> TABLE(id uuid, proveedor_id uuid, proveedor_nombre text, proveedor_ruc text, tipo text, documento text, fecha_emision date, ubicaciones_destino uuid[], estado text, nota text, created_at timestamp with time zone, facturado_cantidad integer, recibido_cantidad integer, estado_recepcion text, fecha_estimada_llegada date, recepcion_atrasada boolean, cerrado_cantidad integer, asignado_aqui bigint, recibido_aqui bigint, cerrado_aqui bigint, pendiente_aqui bigint) [definer]
listar_recepciones_compras(p_proveedor_id uuid, p_desde date, p_hasta date, p_busqueda text, p_limite integer) -> TABLE(lote_id uuid, fecha_recepcion timestamp with time zone, ubicacion_nombre text, proveedor_id uuid, proveedor_nombre text, numero_guia text, recibido_por uuid, compra_id uuid, documento text, unidades_llegaron integer, unidades_facturadas integer, faltante integer, dias_demora integer) [definer]
marcar_comprobante_no_emitido(p_comprobante_id uuid, p_motivo text) -> void [definer]
marcar_egreso_no_gasto(p_caja_movimiento_id uuid, p_tipo text, p_motivo text) -> uuid [definer]
marcar_pedido_no_atendido_resuelto(p_pedido_id uuid) -> void [definer]
marcar_proforma_cobrada(p_proforma_id uuid, p_venta_id uuid) -> void [definer]
marcar_revisados_dinero(p_cuenta_id uuid, p_lineas jsonb, p_revisado boolean) -> integer [definer]
mover_interno(p_ubicacion_id uuid, p_variante_id uuid, p_cantidad integer, p_sububicacion_origen_id uuid, p_sububicacion_destino_id uuid, p_nota text) -> uuid [definer]
notas_credito_tablero() -> TABLE(clase text, id uuid, compra_id uuid, documento text, proveedor_id uuid, proveedor_nombre text, serie_numero text, fecha date, monto numeric, aplicado numeric, a_favor numeric, igv numeric, motivo text, nota text, cierre_id uuid, compra_total numeric, compra_saldo numeric, compra_fecha_emision date, compra_estado text, unidades_cerradas integer, monto_esperado numeric, cerrado_en timestamp with time zone, created_at timestamp with time zone, resuelto boolean) [definer]
por_pagar_tramos(p_proveedor_id uuid, p_condicion text, p_solo_vencidas boolean, p_busqueda text, p_tipo text, p_desde date, p_hasta date, p_ubicacion_id uuid) -> TABLE(tramo text, comprobantes integer, saldo numeric) [definer]
previsualizar_cierre_conteo(p_conteo_id uuid) -> TABLE(variante_id uuid, codigo text, referencia text, talla text, color text, contada integer, sistema integer, diferencia integer, origen text) [definer]
quitar_colaborador(p_persona_id uuid) -> void [definer]
quitar_comprador_de_tienda(p_persona_id uuid, p_ubicacion_id uuid) -> void [definer]
reabrir_periodo(p_mes date, p_alcance text, p_ubicacion_id uuid, p_motivo text) -> void [definer]
reactivar_categoria(p_categoria_id uuid) -> void [definer]
reactivar_colaborador(p_persona_id uuid) -> void [definer]
reactivar_proveedor(p_proveedor_id uuid) -> void [definer]
reactivar_terminal(p_terminal_id uuid) -> void [definer]
reasignar_reparto_compra(p_compra_item_id uuid, p_desde uuid, p_hacia uuid, p_cantidad integer, p_motivo text, p_nota text) -> uuid [definer]
recalcular_compras() -> void [definer]
recalcular_stock() -> void [definer]
recepciones_sin_comprobante(p_ubicacion_id uuid, p_limite integer) -> TABLE(lote_id uuid, fecha_recepcion timestamp with time zone, ubicacion_nombre text, proveedor_nombre text, numero_guia text, nota text, recibido_por uuid, unidades integer, costo_unitario_promedio numeric, sin_costo boolean) [definer]
rechazar_devolucion(p_devolucion_id uuid, p_motivo text) -> void [definer]
recibir_compras(p_ubicacion_id uuid, p_items jsonb, p_numero_guia text, p_nota text) -> uuid [definer]
recibir_comprobante_produccion(p_comprobante_id uuid, p_ubicacion_id uuid, p_lineas jsonb, p_cierres jsonb, p_nota text, p_token uuid) -> uuid [definer]
recibir_envio(p_ubicacion_id uuid, p_items jsonb, p_extras jsonb, p_traslados jsonb, p_cierres jsonb, p_notas_credito jsonb, p_numero_guia text, p_nota text, p_token uuid) -> jsonb [definer]
recibir_insumo(p_insumo_id uuid, p_ubicacion_id uuid, p_cantidad numeric, p_costo_total numeric, p_codigo_lote text, p_proveedor_id uuid, p_documento text, p_origen text, p_nota text, p_token uuid) -> uuid [definer]
recibir_lote(p_ubicacion_id uuid, p_proveedor_id uuid, p_items jsonb, p_numero_guia text, p_nota text, p_token uuid) -> uuid [definer]
recibir_y_cerrar_compras(p_ubicacion_id uuid, p_items jsonb, p_cierres jsonb, p_notas_credito jsonb, p_numero_guia text, p_nota text) -> jsonb [definer]
registrar_activo(p_ubicacion_id uuid, p_tipo text, p_nombre text, p_fecha date, p_monto_total numeric, p_comprobante jsonb, p_medio_pago text, p_caja_id uuid, p_caja_movimiento_id uuid, p_referencia text, p_vida_util_meses integer, p_serie text, p_token uuid, p_cuenta_id uuid) -> uuid [definer]
registrar_adjunto_compra(p_compra_id uuid, p_ruta text, p_nombre text, p_tipo text, p_bytes integer, p_nota_credito_id uuid) -> uuid [definer]
registrar_cambio(p_venta_item_id uuid, p_ubicacion_id uuid, p_variante_nueva_id uuid, p_cantidad integer, p_metodo_pago_diferencia text, p_token uuid, p_motivo text, p_condicion text) -> uuid [definer]
registrar_cambio_clave_terminal(p_terminal_id uuid) -> void [definer]
registrar_clienta(p_dni text, p_nombre text, p_telefono_whatsapp text, p_acepta_whatsapp boolean, p_cumple_dia smallint, p_cumple_mes smallint) -> uuid [definer]
registrar_compra(p_proveedor_id uuid, p_serie text, p_numero text, p_condicion text, p_ubicacion_destino_id uuid, p_items jsonb, p_tipo text, p_fecha_emision date, p_fecha_vencimiento date, p_igv_porcentaje numeric, p_pago jsonb, p_nota text, p_total numeric, p_token uuid, p_fecha_estimada_llegada date) -> uuid [definer]
registrar_comprobante_produccion(p_proveedor_id uuid, p_serie text, p_numero text, p_condicion text, p_items jsonb, p_tipo text, p_fecha_emision date, p_fecha_vencimiento date, p_igv_porcentaje numeric, p_pago jsonb, p_nota text, p_total numeric, p_token uuid) -> uuid [definer]
registrar_conciliacion(p_cuenta_id uuid, p_fecha date, p_saldo_banco numeric, p_nota text) -> uuid [definer]
registrar_consumo_insumo(p_produccion_id uuid, p_insumo_id uuid, p_cantidad numeric, p_nota text) -> uuid [definer]
registrar_devolucion_separacion(p_separacion_id uuid, p_medio text, p_operacion text, p_cci text, p_cuenta_id uuid) -> jsonb [definer]
registrar_gasto(p_ubicacion_id uuid, p_categoria text, p_descripcion text, p_fecha date, p_monto_total numeric, p_comprobante jsonb, p_medio_pago text, p_caja_id uuid, p_caja_movimiento_id uuid, p_referencia text, p_token uuid, p_gasto_fijo_id uuid, p_cuenta_id uuid) -> uuid [definer]
registrar_gasto_legado_2026_09(p_ubicacion_id uuid, p_categoria text, p_total numeric, p_metodo_pago text, p_proveedor_id uuid, p_documento_tipo text, p_documento_serie text, p_documento_numero text, p_igv numeric, p_especificacion text, p_token uuid) -> uuid [definer]
registrar_movimiento(p_variante_id uuid, p_ubicacion_id uuid, p_tipo text, p_cantidad integer, p_motivo text, p_nota text, p_sububicacion_id uuid) -> uuid [definer]
registrar_movimiento_caja(p_caja_id uuid, p_tipo text, p_monto numeric, p_motivo text, p_nota text, p_es_ajuste boolean, p_token uuid) -> uuid [definer]
registrar_movimiento_dinero(p_tipo text, p_monto numeric, p_cuenta_origen_id uuid, p_cuenta_destino_id uuid, p_fecha date, p_referencia text, p_comision numeric, p_caja_id uuid, p_caja_movimiento_id uuid, p_token uuid) -> uuid [definer]
registrar_nota_credito_compra(p_compra_id uuid, p_serie_numero text, p_fecha date, p_monto numeric, p_motivo text, p_nota text, p_cierre_id uuid, p_destino text, p_reembolso_metodo text, p_reembolso_fecha date, p_reembolso_referencia text, p_reembolso_cuenta_id uuid) -> uuid [definer]
registrar_pago_compra(p_compra_id uuid, p_monto numeric, p_metodo text, p_referencia text, p_fecha date) -> uuid [definer]
registrar_pago_compras(p_proveedor_id uuid, p_metodo text, p_aplicaciones jsonb, p_referencia text, p_fecha date, p_token uuid, p_credito numeric, p_ubicacion_id uuid) -> uuid [definer]
registrar_pago_compras_medios(p_proveedor_id uuid, p_aplicaciones jsonb, p_medios jsonb, p_fecha date, p_token uuid, p_credito numeric, p_ubicacion_id uuid) -> uuid [definer]
registrar_pago_comprobante_produccion(p_comprobante_id uuid, p_pagos jsonb, p_fecha date, p_token uuid) -> uuid [definer]
registrar_pagos_compra(p_compra_id uuid, p_pagos jsonb, p_fecha date, p_token uuid, p_ubicacion_id uuid) -> uuid[] [definer]
registrar_pedido_no_atendido(p_ubicacion_id uuid, p_producto_id uuid, p_descripcion_libre text, p_talla text, p_clienta_id uuid) -> uuid [definer]
registrar_proveedor(p_nombre text, p_ruc text, p_contacto text, p_rubro text, p_plazo_credito_dias integer, p_forma_pago_preferida text, p_telefono text, p_banco text, p_cuenta_bancaria text) -> uuid [definer]
registrar_proveedor_de_gasto(p_nombre text, p_ruc text) -> uuid [definer]
registrar_recepcion_traslado(p_transferencia_id uuid, p_variante_id uuid, p_cantidad_recibida integer) -> uuid [definer]
registrar_reembolso_proveedor(p_proveedor_id uuid, p_monto numeric, p_metodo text, p_referencia text, p_fecha date, p_nota text, p_cuenta_id uuid) -> uuid [definer]
registrar_saldo_inicial(p_fecha date, p_lineas jsonb, p_motivo text) -> integer [definer]
registrar_serie_comprobante(p_ubicacion_id uuid, p_tipo text, p_serie text, p_siguiente_numero integer) -> uuid [definer]
registrar_venta(p_ubicacion_id uuid, p_items jsonb, p_pagos jsonb, p_cliente_id uuid, p_token uuid, p_tipo_comprobante text, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text, p_codigo_descuento text, p_nota text, p_asesora_id uuid, p_emisor text, p_descuento_pct numeric, p_autorizado_por uuid, p_motivo_descuento text) -> uuid [definer]
regularizar_prenda(p_id uuid, p_variante_id uuid, p_forma text) -> numeric [definer]
renombrar_rol(p_rol_id uuid, p_nombre text, p_descripcion text) -> void [definer]
resolver_prenda_danada(p_id uuid, p_estado text, p_nota text, p_proveedor_id uuid) -> void [definer]
restaurar_rol(p_rol_id uuid) -> void [definer]
resumen_compras() -> TABLE(registradas bigint, vigentes bigint, por_recibir bigint, deuda numeric, con_saldo bigint, vencido numeric, vencidas bigint, por_vencer bigint, por_vencer_monto numeric, por_recibir_atrasadas bigint) [definer]
resumen_compras_extra() -> TABLE(unidades_pendientes bigint, valor_por_recibir numeric, dias_mas_atrasada integer, documento_mas_atrasada text, proveedor_mas_atrasado text, compras_mes numeric, compras_mes_anterior numeric, igv_mes numeric, top_proveedor_id uuid, top_proveedor_nombre text, top_proveedor_pct numeric) [definer]
resumen_recepciones(p_desde date) -> TABLE(unidades_recibidas bigint, recepciones integer, dias_entrega_promedio numeric, comprobantes_recibidos integer, entregas_completas integer, faltante_unidades bigint, faltante_comprobantes integer) [definer]
resumen_separaciones(p_ubicacion_id uuid) -> TABLE(por_recoger integer, prendas_guardadas integer, en_custodia numeric, en_custodia_efectivo numeric, por_devolver integer, monto_por_devolver numeric, vencen_pronto integer, vencidas integer) [definer]
resumen_sin_comprobante(p_ubicacion_id uuid) -> TABLE(unidades_mes bigint, recepciones_mes integer, unidades_sin_costo_mes bigint, ultima_recepcion timestamp with time zone, ultima_ubicacion text) [definer]
revertir_egreso_no_gasto(p_id uuid) -> void [definer]
revertir_produccion(p_produccion_id uuid) -> void [definer]
revisar_apertura_caja(p_caja_id uuid) -> void [definer]
revisar_producto_censo(p_producto_id uuid, p_aprobar boolean) -> void [definer]
salidas_caja_30d() -> TABLE(orden integer, etiqueta text, desde date, hasta date, comprobantes integer, monto numeric, es_vencido boolean) [definer]
separar_prendas(p_ubicacion_id uuid, p_items jsonb, p_pagos jsonb, p_clienta_nombres text, p_clienta_apellidos text, p_clienta_celular text, p_devolucion_medio text, p_clienta_dni text, p_devolucion_numero text, p_devolucion_cci text, p_comprobante_tipo text, p_cliente_ruc text, p_cliente_razon_social text, p_asesora_id uuid, p_clienta_id uuid, p_nota text, p_token uuid) -> uuid [definer]
set_etapa_produccion(p_produccion_id uuid, p_etapa text, p_estado text) -> void [definer]
suspender_colaborador(p_persona_id uuid, p_motivo text) -> void [definer]
