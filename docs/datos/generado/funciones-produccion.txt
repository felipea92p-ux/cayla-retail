abrir_caja(p_ubicacion_id uuid, p_monto_apertura numeric) -> uuid [definer]
abrir_conteo(p_ubicacion_id uuid, p_sububicacion_id uuid, p_alcance text, p_alcance_categoria_id uuid) -> uuid [definer]
abrir_produccion(p_ubicacion_id uuid, p_producto_id uuid, p_lineas jsonb, p_costo_tela numeric, p_costo_avios numeric, p_costo_maquila numeric, p_es_muestra boolean, p_fecha_entrega date, p_nota text, p_token uuid) -> uuid [definer]
actualizar_categoria(p_categoria_id uuid, p_nombre text, p_familia text, p_prefijo text, p_notas text) -> void [definer]
actualizar_categoria_ejes(p_categoria_id uuid, p_talla_ids uuid[], p_tejido_ids uuid[], p_patron_ids uuid[]) -> void [definer]
actualizar_mi_foto_perfil(p_foto_url text) -> void [definer]
actualizar_proveedor(p_proveedor_id uuid, p_nombre text, p_ruc text, p_contacto text) -> void [definer]
actualizar_transmision_comprobante(p_comprobante_id uuid, p_estado text, p_entorno text, p_respuesta_sunat jsonb, p_motivo_rechazo text) -> void [definer]
actualizar_variantes_etiquetas(p_asignaciones jsonb) -> void [definer]
agregar_colaborador(p_persona_id uuid, p_ubicacion_id uuid) -> void [definer]
ajustar_insumo_por_conteo(p_insumo_id uuid, p_ubicacion_id uuid, p_cantidad_contada numeric, p_motivo text) -> uuid [definer]
anular_compra(p_compra_id uuid, p_motivo text) -> void [definer]
anular_comprobante(p_comprobante_id uuid, p_motivo text, p_confirmada boolean, p_respuesta jsonb) -> void [definer]
anular_conteo(p_conteo_id uuid) -> void [definer]
anular_produccion(p_produccion_id uuid, p_motivo text) -> void [definer]
anular_venta(p_venta_id uuid, p_motivo text, p_items jsonb) -> void [definer]
aprobar_devolucion(p_devolucion_id uuid, p_reembolso_monto numeric, p_reembolso_metodo text) -> void [definer]
archivar_adjunto_compra(p_adjunto_id uuid) -> void [definer]
catalogo_actualizar_producto(p_producto_id uuid, p_referencia text, p_estado text, p_variantes jsonb, p_categoria_id uuid, p_descripcion text, p_stock_minimo integer, p_temporada text, p_permitir_venta_sin_stock boolean, p_fotos jsonb, p_tejido_id uuid, p_patron_id uuid) -> void [invoker]
catalogo_crear_producto(p_referencia text, p_variantes jsonb, p_categoria_id uuid, p_descripcion text, p_stock_minimo integer, p_temporada text, p_permitir_venta_sin_stock boolean, p_fotos jsonb, p_tejido_id uuid, p_patron_id uuid) -> uuid [invoker]
cerrar_caja(p_caja_id uuid, p_monto_real numeric) -> TABLE(monto_sistema numeric, monto_real numeric, diferencia numeric) [definer]
cerrar_conteo(p_conteo_id uuid) -> TABLE(lineas_ajustadas integer, unidades_sobrantes integer, unidades_faltantes integer) [definer]
cerrar_produccion(p_produccion_id uuid, p_buenas jsonb, p_costo_tela numeric, p_costo_avios numeric, p_costo_maquila numeric) -> void [definer]
cerrar_traslado_con_diferencia(p_transferencia_id uuid, p_nota text) -> TABLE(lineas_recibidas integer, unidades_recibidas integer) [definer]
confirmar_traslado(p_transferencia_id uuid) -> TABLE(resultado text, lineas_ok integer, lineas_con_diferencia integer) [definer]
conteo_contar(p_conteo_id uuid, p_variante_id uuid, p_cantidad_contada integer) -> uuid [definer]
convertir_proforma_a_comprobante(p_proforma_id uuid, p_tipo text, p_venta_id uuid, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text) -> uuid [definer]
crear_devolucion(p_venta_id uuid, p_ubicacion_id uuid, p_items jsonb, p_motivo text) -> uuid [definer]
crear_producto_con_variantes(p_referencia text, p_categoria_id uuid, p_variantes jsonb, p_descripcion text, p_token uuid, p_tejido_id uuid, p_patron_id uuid) -> uuid [definer]
crear_proforma(p_ubicacion_id uuid, p_items jsonb, p_subtotal numeric, p_igv numeric, p_total numeric, p_cliente_nombre text, p_cliente_num_doc text, p_vence_at timestamp with time zone) -> uuid [definer]
desactivar_categoria(p_categoria_id uuid) -> void [definer]
desactivar_proveedor(p_proveedor_id uuid) -> void [definer]
emitir_comprobante(p_ubicacion_id uuid, p_tipo text, p_subtotal numeric, p_igv numeric, p_total numeric, p_venta_id uuid, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text, p_items jsonb) -> uuid [definer]
emitir_nota(p_comprobante_original_id uuid, p_tipo text, p_motivo text, p_subtotal numeric, p_igv numeric, p_total numeric, p_items jsonb) -> uuid [definer]
fn_activos_fijos_set_updated_at() -> trigger [invoker]
fn_aplicar_movimiento(p_movimiento_id uuid) -> void [definer]
fn_asignar_codigo_producto(p_producto_id uuid) -> text [definer]
fn_asignar_codigo_variante(p_variante_id uuid) -> text [definer]
fn_clave_texto(p text) -> text [invoker]
fn_colaboradores() -> TABLE(persona_id uuid, nombre text, correo text, sede text, rol text, ubicacion_asignada text, agregado_en timestamp with time zone) [definer]
fn_colores_estado_trigger() -> trigger [definer]
fn_compra_item_insertado() -> trigger [definer]
fn_compra_pago_insertado() -> trigger [definer]
fn_conteos_resumen(p_ubicacion_id uuid, p_limite integer) -> TABLE(id uuid, numero integer, estado text, created_at timestamp with time zone, cerrado_en timestamp with time zone, sububicacion_id uuid, sububicacion_nombre text, sububicacion_tipo text, alcance text, alcance_categoria_nombre text, abierto_por uuid, cerrado_por uuid, lineas integer, lineas_con_diferencia integer, sistema integer, contado integer, diferencia integer, soles_diferencia numeric) [invoker]
fn_costo_historial(p_variante_id uuid) -> TABLE(id uuid, created_at timestamp with time zone, stock_previo integer, costo_anterior numeric, cantidad_nueva integer, costo_unitario_nuevo numeric, costo_resultante numeric, origen text, usuario_nombre text, lote_guia text, proveedor_nombre text, compra_documento text, produccion_referencia text) [definer]
fn_costo_historial_inmutable() -> trigger [invoker]
fn_dynamic_disponibles() -> TABLE(persona_id uuid, nombre text, correo text, sede text) [definer]
fn_es_lider() -> boolean [definer]
fn_etiquetas_estado_trigger() -> trigger [definer]
fn_historial_es_inmutable() -> trigger [invoker]
fn_historial_producto_cambios(p_producto_id uuid) -> TABLE(id uuid, created_at timestamp with time zone, entidad text, campo text, valor_anterior text, valor_nuevo text, categoria_anterior_nombre text, categoria_nueva_nombre text, variante_id uuid, variante_sku text, variante_talla text, variante_color text, usuario_id uuid, usuario_nombre text) [definer]
fn_historial_producto_cambios_inmutable() -> trigger [invoker]
fn_historial_sin_truncate() -> trigger [invoker]
fn_insumos_set_updated_at() -> trigger [invoker]
fn_linea_de_venta_no_anulada() -> trigger [definer]
fn_mi_perfil() -> TABLE(persona_id uuid, nombres text, apellidos text, correo text, celular text, foto_url text, rol text, estado text, ubicacion_nombre text, ultimo_acceso timestamp with time zone) [definer]
fn_movimiento_compra_insertado() -> trigger [definer]
fn_movimientos(p_ubicacion_id uuid, p_desde date, p_hasta date, p_categoria text, p_motivo text, p_busqueda text, p_usuario_id uuid, p_sububicacion_id uuid, p_cursor_creado_en timestamp with time zone, p_cursor_id uuid, p_limite integer, p_producto_id uuid) -> TABLE(id uuid, created_at timestamp with time zone, fecha_lima date, hora text, tipo text, categoria text, motivo text, cantidad integer, delta integer, es_sistema boolean, nota text, variante_id uuid, sku text, referencia text, talla text, color text, ubicacion_id uuid, ubicacion_nombre text, ubicacion_destino_id uuid, ubicacion_destino_nombre text, sububicacion_id uuid, sububicacion_nombre text, sububicacion_tipo text, sububicacion_destino_id uuid, sububicacion_destino_nombre text, sububicacion_destino_tipo text, usuario_id uuid, usuario_nombre text, venta_id uuid, venta_nota text, comprobante_tipo text, comprobante_numero text, comprobante_estado text, lote_id uuid, lote_guia text, lote_nota text, proveedor_nombre text, compra_id uuid, compra_documento text, transferencia_id uuid, transferencia_estado text, transferencia_nota text, conteo_id uuid, conteo_cantidad_sistema integer, conteo_cantidad_contada integer, devolucion_id uuid, devolucion_motivo text, devolucion_estado text, cambio_id uuid, cambio_diferencia numeric) [definer]
fn_movimientos_resumen(p_ubicacion_id uuid, p_desde date, p_hasta date, p_motivo text, p_busqueda text, p_usuario_id uuid, p_sububicacion_id uuid) -> TABLE(categoria text, movimientos bigint, unidades bigint, delta bigint) [definer]
fn_movimientos_variantes(p_busqueda text) -> uuid[] [invoker]
fn_nombres_personas(p_ids uuid[]) -> TABLE(id uuid, nombre text) [definer]
fn_patrones_estado_trigger() -> trigger [definer]
fn_persona_actual_resumen() -> TABLE(nombre text, es_lider boolean, ubicacion_id uuid, ubicacion_nombre text, ubicacion_tipo text) [definer]
fn_prioridad_conteo(p_ubicacion_id uuid, p_alcance_categoria_id uuid) -> TABLE(variante_id uuid, sku text, referencia text, talla text, color text, dias_sin_contar integer, valor_en_riesgo numeric) [definer]
fn_productos(p_busqueda text, p_categoria_id uuid, p_color_codigo text, p_estado text, p_precio_min numeric, p_precio_max numeric, p_stock text, p_pagina integer, p_por_pagina integer, p_orden text) -> TABLE(total_productos bigint, producto_id uuid, referencia text, codigo text, categoria_id uuid, categoria_nombre text, estado text, stock_minimo integer, stock_total integer, demanda_diaria numeric, lead_time_dias numeric, punto_reorden integer, reponer_de_proveedor boolean, variante_id uuid, variante_codigo text, sku text, talla text, color_codigo text, color_nombre text, color_hex text, foto_url text, precio numeric, costo numeric, activo boolean, codigos_barras text[]) [definer]
fn_productos_buscar(p_busqueda text) -> uuid[] [invoker]
fn_productos_resumen(p_busqueda text, p_categoria_id uuid, p_color_codigo text, p_estado text, p_precio_min numeric, p_precio_max numeric) -> TABLE(total_productos bigint, total_variantes bigint, stock_bajo bigint, sin_stock bigint, reponer_de_proveedor bigint) [definer]
fn_proveedores() -> TABLE(id uuid, nombre text, ruc text, contacto text, activo boolean, facturas bigint, saldo numeric, ultima_compra date) [definer]
fn_puede_operar_ubicacion(p_ubicacion_id uuid) -> boolean [invoker]
fn_puede_registrar_compras() -> boolean [invoker]
fn_recalcular_costo_variante(p_variante_id uuid, p_cantidad_nueva integer, p_costo_unitario_nuevo numeric, p_origen text, p_movimiento_id uuid) -> numeric [definer]
fn_registrar_cambio_producto() -> trigger [definer]
fn_reservar_numero_serie(p_ubicacion_id uuid, p_tipo text) -> TABLE(serie text, numero integer) [definer]
fn_siguiente_correlativo(p_prefijo text) -> integer [definer]
fn_stock_por_sede() -> TABLE(variante_id uuid, ubicacion_id uuid, cantidad integer) [definer]
fn_sububicacion_por_defecto(p_ubicacion_id uuid, p_uso text) -> uuid [invoker]
fn_tallas_estado_trigger() -> trigger [definer]
fn_tejidos_estado_trigger() -> trigger [definer]
fn_texto_o_null(p text) -> text [invoker]
fn_tiene_acceso_retail() -> boolean [definer]
fn_token_talla(p_talla text) -> text [invoker]
fn_traslado_lineas(p_transferencia_id uuid) -> TABLE(variante_id uuid, sku text, referencia text, talla text, color text, cantidad_enviada integer, cantidad_recibida integer, diferencia integer) [definer]
fn_ubicacion_actual_persona() -> uuid [definer]
fn_valida_categoria_subcategoria() -> trigger [invoker]
fn_valida_nota_referencia_aceptada() -> trigger [invoker]
fn_variante_permitida_en_sede(p_variante_id uuid, p_ubicacion_id uuid) -> boolean [invoker]
fn_variantes_asignar_codigo() -> trigger [invoker]
fn_ventas_del_dia(p_ubicacion_id uuid) -> TABLE(venta_id uuid, hora text, ubicacion_nombre text, vendedor text, cliente_nombre text, items jsonb, total numeric, metodos_pago text, comprobante_tipo text, comprobante_texto text, comprobante_estado text, nota text) [definer]
iniciar_traslado(p_ubicacion_origen_id uuid, p_ubicacion_destino_id uuid, p_items jsonb, p_fecha_estimada_llegada timestamp with time zone, p_nota text) -> uuid [definer]
liquidar_prenda_danada(p_id uuid, p_precio_unitario numeric, p_metodo_pago text, p_nota text) -> uuid [definer]
listar_compras(p_limite integer, p_cursor_fecha date, p_cursor_creado_en timestamp with time zone, p_cursor_id uuid, p_orden text, p_busqueda text, p_proveedor_id uuid, p_estado_pago text, p_estado_recepcion text, p_condicion text, p_solo_vigentes boolean, p_con_saldo boolean, p_solo_vencidas boolean, p_por_recibir boolean, p_desde date, p_hasta date) -> SETOF retail.compras_resumen [invoker]
marcar_comprobante_no_emitido(p_comprobante_id uuid, p_motivo text) -> void [definer]
mover_interno(p_ubicacion_id uuid, p_variante_id uuid, p_cantidad integer, p_sububicacion_origen_id uuid, p_sububicacion_destino_id uuid, p_nota text) -> uuid [definer]
previsualizar_cierre_conteo(p_conteo_id uuid) -> TABLE(variante_id uuid, codigo text, referencia text, talla text, color text, contada integer, sistema integer, diferencia integer, origen text) [definer]
quitar_colaborador(p_persona_id uuid) -> void [definer]
reactivar_categoria(p_categoria_id uuid) -> void [definer]
reactivar_proveedor(p_proveedor_id uuid) -> void [definer]
recalcular_compras() -> void [definer]
recalcular_stock() -> void [definer]
rechazar_devolucion(p_devolucion_id uuid, p_motivo text) -> void [definer]
recibir_compras(p_ubicacion_id uuid, p_items jsonb, p_numero_guia text, p_nota text) -> uuid [definer]
recibir_insumo(p_insumo_id uuid, p_ubicacion_id uuid, p_cantidad numeric, p_costo_total numeric, p_codigo_lote text, p_proveedor_id uuid, p_documento text, p_origen text, p_nota text) -> uuid [definer]
recibir_lote(p_ubicacion_id uuid, p_proveedor_id uuid, p_items jsonb, p_numero_guia text, p_nota text) -> uuid [definer]
registrar_adjunto_compra(p_compra_id uuid, p_ruta text, p_nombre text, p_tipo text, p_bytes integer) -> uuid [definer]
registrar_cambio(p_venta_item_id uuid, p_ubicacion_id uuid, p_variante_nueva_id uuid, p_cantidad integer, p_metodo_pago_diferencia text, p_token uuid) -> uuid [definer]
registrar_compra(p_proveedor_id uuid, p_serie text, p_numero text, p_condicion text, p_ubicacion_destino_id uuid, p_items jsonb, p_tipo text, p_fecha_emision date, p_fecha_vencimiento date, p_igv_porcentaje numeric, p_pago jsonb, p_nota text, p_total numeric, p_token uuid) -> uuid [definer]
registrar_consumo_insumo(p_produccion_id uuid, p_insumo_id uuid, p_cantidad numeric, p_nota text) -> uuid [definer]
registrar_gasto(p_ubicacion_id uuid, p_categoria text, p_total numeric, p_metodo_pago text, p_proveedor_id uuid, p_documento_tipo text, p_documento_serie text, p_documento_numero text, p_igv numeric, p_especificacion text, p_token uuid) -> uuid [definer]
registrar_movimiento(p_variante_id uuid, p_ubicacion_id uuid, p_tipo text, p_cantidad integer, p_motivo text, p_nota text, p_sububicacion_id uuid) -> uuid [definer]
registrar_movimiento_caja(p_caja_id uuid, p_tipo text, p_monto numeric, p_motivo text, p_nota text, p_es_ajuste boolean) -> uuid [definer]
registrar_pago_compra(p_compra_id uuid, p_monto numeric, p_metodo text, p_referencia text, p_fecha date) -> uuid [definer]
registrar_pagos_compra(p_compra_id uuid, p_pagos jsonb, p_fecha date) -> uuid[] [definer]
registrar_proveedor(p_nombre text, p_ruc text, p_contacto text) -> uuid [definer]
registrar_recepcion_traslado(p_transferencia_id uuid, p_variante_id uuid, p_cantidad_recibida integer) -> uuid [definer]
registrar_serie_comprobante(p_ubicacion_id uuid, p_tipo text, p_serie text, p_siguiente_numero integer) -> uuid [definer]
registrar_venta(p_ubicacion_id uuid, p_items jsonb, p_pagos jsonb, p_cliente_id uuid, p_token uuid, p_tipo_comprobante text, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text, p_codigo_descuento text, p_nota text) -> uuid [definer]
resolver_prenda_danada(p_id uuid, p_estado text, p_nota text) -> void [definer]
resumen_compras() -> TABLE(registradas bigint, vigentes bigint, por_recibir bigint, deuda numeric, con_saldo bigint, vencido numeric, vencidas bigint, por_vencer bigint, por_vencer_monto numeric) [definer]
revertir_produccion(p_produccion_id uuid) -> void [definer]
set_etapa_produccion(p_produccion_id uuid, p_etapa text, p_estado text) -> void [definer]
