abrir_caja(p_sede_id uuid, p_monto_apertura numeric) -> uuid [definer]
abrir_conteo(p_sede_id uuid, p_alcance text, p_alcance_familia text, p_alcance_categoria_id uuid, p_alcance_contenedor_id uuid, p_ubicacion text, p_nombre text) -> uuid [definer]
actualizar_transmision_comprobante(p_comprobante_id uuid, p_estado text, p_entorno text, p_respuesta_sunat jsonb, p_motivo_rechazo text) -> void [definer]
anular_comprobante(p_comprobante_id uuid, p_motivo text, p_confirmada boolean, p_respuesta jsonb) -> void [definer]
anular_conteo(p_conteo_id uuid, p_motivo text) -> void [definer]
bajar_a_piso(p_sede_id uuid, p_variante_id uuid, p_cantidad integer, p_nota text) -> uuid [definer]
catalogo_con_stock() -> TABLE(...) [invoker]
cerrar_caja(p_caja_id uuid, p_monto_contado numeric) -> TABLE(monto_esperado numeric, monto_contado numeric, diferencia numeric) [definer]
cerrar_conteo(p_conteo_id uuid) -> TABLE(lineas_totales integer, lineas_ajustadas integer, unidades_sobrantes integer, unidades_faltantes integer) [definer]
cerrar_produccion(p_produccion_id uuid, p_costo_tela numeric, p_costo_avios numeric, p_costo_maquila numeric, p_buenas jsonb) -> void [definer]
conteo_contar(p_conteo_id uuid, p_variante_id uuid, p_cantidad integer, p_modo text, p_contenedor_id uuid) -> uuid [definer]
conteo_contar_por_codigo(p_conteo_id uuid, p_codigo text, p_cantidad integer, p_modo text) -> uuid [definer]
conteo_crear_variante(p_conteo_id uuid, p_referencia text, p_talla text, p_color_codigo text, p_cantidad integer, p_categoria_id uuid, p_producto_id uuid, p_precio numeric, p_costo numeric, p_codigo_barras text, p_sku text, p_contenedor_id uuid) -> uuid [definer]
convertir_proforma_a_comprobante(p_proforma_id uuid, p_tipo text, p_venta_id uuid, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text) -> uuid [definer]
crear_producto_con_variantes(p_sku_padre text, p_referencia text, p_variantes jsonb, p_categoria_id uuid, p_genero text, p_marca text, p_temporada text, p_proveedor_id uuid) -> uuid [definer]
crear_proforma(p_sede_id uuid, p_items jsonb, p_subtotal numeric, p_igv numeric, p_total numeric, p_cliente_nombre text, p_cliente_num_doc text, p_vence_at timestamp with time zone) -> uuid [definer]
deshacer_importacion(p_importacion_id uuid) -> jsonb [definer]
devolver_a_almacen(p_sede_id uuid, p_variante_id uuid, p_cantidad integer, p_nota text) -> uuid [definer]
eliminar_produccion(p_produccion_id uuid) -> void [definer]
emitir_comprobante(p_sede_id uuid, p_tipo text, p_subtotal numeric, p_igv numeric, p_total numeric, p_venta_id uuid, p_cliente_tipo_doc text, p_cliente_num_doc text, p_cliente_nombre text, p_items jsonb) -> uuid [definer]
emitir_nota(p_comprobante_original_id uuid, p_tipo text, p_motivo text, p_subtotal numeric, p_igv numeric, p_total numeric, p_items jsonb) -> uuid [definer]
es_lider() -> boolean [invoker]
es_supervisor() -> boolean [invoker]
fijar_stock_minimo(p_variante_id uuid, p_sede_id uuid, p_minimo integer) -> void [definer]
fn_aplicar_movimiento(p_movimiento_id uuid) -> void [definer]
fn_asiento_cuadra() -> trigger [invoker]
fn_asignar_codigo_producto(p_producto_id uuid) -> text [definer]
fn_asignar_codigo_variante(p_variante_id uuid) -> text [definer]
fn_clave_texto(p text) -> text [invoker]
fn_codigo_tres_letras(p_nombre text, p_tabla text) -> text [invoker]
fn_componer_codigo_variante(p_base text, p_color_id text, p_talla text) -> text [invoker]
fn_familia_color_de_universal(p_valor_id text) -> text [invoker]
fn_familia_de_universal(p_categoria_id text) -> text [invoker]
fn_normalizar_color(p_color_id text, p_color text, OUT color_id text, OUT color text) -> record [invoker]
fn_reservar_numero_serie(p_sede_id uuid, p_tipo text) -> TABLE(serie text, numero integer) [definer]
fn_siguiente_correlativo(p_prefijo text) -> integer [definer]
fn_token_talla(p_talla text) -> text [invoker]
fn_valida_nota_referencia_aceptada() -> trigger [invoker]
importar_catalogo(p_catalogo jsonb) -> jsonb [definer]
mi_sede() -> uuid [invoker]
persona_actual() -> TABLE(id uuid, auth_user_id uuid, nombre text, sede_id uuid, rol text, email text) [definer]
previsualizar_cierre_conteo(p_conteo_id uuid) -> TABLE(...) [definer]
puede_operar_sede(p_sede_id uuid) -> boolean [invoker]
recalcular_stock() -> void [definer]
recibir_lote(p_sede_id uuid, p_origen text, p_items jsonb, p_proveedor text, p_numero_guia text, p_nota text, p_orden_compra_id uuid) -> uuid [definer]
registrar_asiento(p_unidad_id uuid, p_glosa text, p_origen text, p_lineas jsonb, p_fecha date, p_referencia_tipo text, p_referencia_id uuid) -> uuid [definer]
registrar_codigo_barras(p_variante_id uuid, p_codigo text, p_origen text, p_nota text) -> uuid [definer]
registrar_deposito(p_sede_id uuid, p_monto numeric, p_nota text, p_fecha date) -> uuid [definer]
registrar_gasto(p_sede_id uuid, p_categoria text, p_subtotal numeric, p_igv numeric, p_total numeric, p_especificacion text) -> uuid [definer]
registrar_movimiento(p_variante_id uuid, p_sede_id uuid, p_tipo text, p_cantidad integer, p_motivo text, p_canal text, p_sede_destino_id uuid, p_monto numeric, p_venta_id uuid, p_nota text, p_contenedor_id uuid, p_lote_id uuid) -> uuid [definer]
registrar_produccion(p_unidad_id uuid, p_cantidad integer, p_costo_tela numeric, p_costo_avios numeric, p_costo_maquila numeric, p_precio_taller numeric, p_variantes jsonb, p_producto_id uuid, p_referencia text, p_categoria_id uuid, p_detalle text, p_es_muestra boolean, p_fecha_entrega date, p_marcar_terminado boolean, p_nota text, p_material text) -> uuid [definer]
registrar_serie_comprobante(p_sede_id uuid, p_tipo text, p_serie text, p_siguiente_numero integer) -> uuid [definer]
registrar_venta(p_caja_id uuid, p_metodo_pago text, p_items jsonb, p_nota text, p_token uuid) -> uuid [definer]
revertir_produccion_inventario(p_produccion_id uuid) -> void [definer]
set_etapa_produccion(p_produccion_id uuid, p_etapa text, p_estado text) -> void [definer]
set_updated_at() -> trigger [invoker]
